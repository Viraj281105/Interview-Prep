export const postgresData = {
  id: 'postgres',
  title: 'PostgreSQL Deep Dive',
  icon: '🐘',
  summary: 'Advanced features of the world\'s most advanced open-source relational database.',
  concepts: ['MVCC', 'JSONB', 'CTEs', 'Window Functions', 'Vacuuming', 'WAL', 'Indexes', 'Full-Text Search', 'Partitioning'],
  questions: [
    { id: 'pg-001', question: 'What makes PostgreSQL different from MySQL?', difficulty: 'medium', type: 'theory', answer: 'PostgreSQL is strictly an Object-Relational Database Management System (ORDBMS). It fully complies with ACID, supports advanced data types (JSONB, Arrays, HStore, UUIDs, Geospatial), advanced locking (MVCC), materialized views, and partial indexes. MySQL is historically geared towards speed and web apps, though it has caught up in recent versions.' },
    { id: 'pg-002', question: 'Explain MVCC (Multi-Version Concurrency Control) in PostgreSQL.', difficulty: 'hard', type: 'theory', answer: 'MVCC allows high concurrency. Instead of locking a row when reading/writing, PostgreSQL creates a new version of the row for the transaction. Readers don\'t block writers, and writers don\'t block readers. Old versions (dead tuples) are kept until transactions that need them finish, then cleaned up by VACUUM.' },
    { id: 'pg-003', question: 'What is VACUUM in PostgreSQL?', difficulty: 'hard', type: 'theory', answer: 'A maintenance process. Because of MVCC, UPDATEs and DELETEs leave behind "dead tuples" (old row versions). `VACUUM` scans tables to reclaim the space taken by these dead tuples for future inserts. `VACUUM FULL` aggressively locks the table and rewrites it to disk to return space to the OS (rarely used). `autovacuum` runs automatically in the background.' },
    { id: 'pg-004', question: 'Explain JSON vs JSONB in Postgres.', difficulty: 'medium', type: 'theory', answer: '`json` stores an exact text copy of the input text. Processing functions must reparse it on every execution. `jsonb` stores data in a decomposed binary format. It is slightly slower to insert, but significantly faster to process, supports advanced indexing (GIN), and removes duplicate keys.' },
    { id: 'pg-005', question: 'What is a GIN Index?', difficulty: 'hard', type: 'theory', answer: 'Generalized Inverted Index. Optimized for data types containing multiple values, such as Arrays, JSONB, and Full-Text Search vectors (`tsvector`). It maps values to the rows where they appear, rather than mapping rows to values.' },
    { id: 'pg-006', question: 'What is an Expression / Function-based Index?', difficulty: 'medium', type: 'practical', answer: 'An index created not on a raw column, but on an expression or function applied to a column. E.g., `CREATE INDEX idx_lower_email ON users (LOWER(email))`. Essential for fast case-insensitive searches.' },
    { id: 'pg-007', question: 'What is a Partial Index?', difficulty: 'medium', type: 'practical', answer: 'An index built over a subset of a table, defined by a `WHERE` clause. E.g., `CREATE INDEX idx_active_users ON users (id) WHERE active = true`. Saves disk space and speeds up inserts/updates compared to indexing the whole table.' },
    { id: 'pg-008', question: 'Explain Write-Ahead Logging (WAL) in Postgres.', difficulty: 'hard', type: 'theory', answer: 'Ensures data integrity. Changes to data files are written only AFTER they have been logged in the WAL. In case of a crash, the DB recovers by replaying the WAL logs. Also used fundamentally for replication (shipping WAL segments to standby servers).' },
    { id: 'pg-009', question: 'What is the RETURNING clause?', difficulty: 'easy', type: 'practical', answer: 'A Postgres-specific extension that allows an `INSERT`, `UPDATE`, or `DELETE` statement to return the values of the modified rows immediately, avoiding a subsequent `SELECT` query. E.g., `INSERT INTO users (name) VALUES (\'John\') RETURNING id;`' },
    { id: 'pg-010', question: 'What are CTEs (Common Table Expressions)?', difficulty: 'medium', type: 'practical', answer: 'Temporary result sets defined by the `WITH` clause, lasting only for the execution of the current query. They make complex queries (especially deeply nested subqueries) much more readable. Also supports recursive queries via `WITH RECURSIVE`.' },
    { id: 'pg-011', question: 'Explain Window Functions.', difficulty: 'hard', type: 'practical', answer: 'Calculations across a set of table rows related to the current row, without collapsing them into a single row like `GROUP BY`. Syntax uses `OVER(PARTITION BY col ORDER BY col)`. Examples: `row_number()`, `rank()`, `lead()`, `lag()`.' },
    { id: 'pg-012', question: 'What is the difference between `LIMIT` and `FETCH FIRST`?', difficulty: 'easy', type: 'theory', answer: '`LIMIT` is historically supported by Postgres (and MySQL/SQLite) but is not SQL standard. `FETCH FIRST n ROWS ONLY` is the ANSI SQL standard equivalent introduced in SQL:2008 and supported by Postgres.' },
    { id: 'pg-013', question: 'How do you perform Full-Text Search in Postgres?', difficulty: 'hard', type: 'practical', answer: 'Using `tsvector` (text parsed into tokens) and `tsquery` (search terms). Create a GIN index on the `tsvector` column. Query using the `@@` match operator: `WHERE to_tsvector(\'english\', body) @@ to_tsquery(\'postgres & search\')`.' },
    { id: 'pg-014', question: 'Explain Table Partitioning.', difficulty: 'hard', type: 'theory', answer: 'Splitting one large logical table into smaller physical pieces (partitions). Postgres supports declarative partitioning by Range (e.g., date ranges), List (e.g., specific country codes), or Hash. Improves query performance and makes archiving old data instantly fast (dropping a partition).' },
    { id: 'pg-015', question: 'What are EXPLAIN and EXPLAIN ANALYZE?', difficulty: 'medium', type: 'practical', answer: '`EXPLAIN` shows the query planner\'s estimated execution plan (cost, rows). `EXPLAIN ANALYZE` actually executes the query and shows the TRUE execution time and rows processed alongside the estimates. Crucial for performance tuning.' },
    { id: 'pg-016', question: 'What is pgBouncer?', difficulty: 'medium', type: 'theory', answer: 'A lightweight connection pooler for PostgreSQL. Postgres connections are heavily process-based (one OS process per connection), taking ~10MB RAM each. pgBouncer maintains a pool of connections and multiplexes thousands of client connections over them, preventing the DB from running out of memory.' },
    { id: 'pg-017', question: 'What is the role of `pg_stat_activity`?', difficulty: 'medium', type: 'practical', answer: 'A system catalog view that shows information related to the current activity of the server process. You query it to see what queries are currently running, who is running them, and if any queries are locked or hanging.' },
    { id: 'pg-018', question: 'What are Foreign Data Wrappers (FDW)?', difficulty: 'hard', type: 'theory', answer: 'An extension mechanism that allows Postgres to connect to and query external data sources (like another Postgres DB, MySQL, MongoDB, or even a CSV file) directly using standard SQL queries, as if they were local tables.' },
    { id: 'pg-019', question: 'Explain the concept of Toast in PostgreSQL.', difficulty: 'expert', type: 'theory', answer: 'The Oversized-Attribute Storage Technique. Postgres pages are 8KB. If a row (e.g., containing a massive JSONB or text field) exceeds this, Postgres compresses and splits the large column into multiple physical rows in a hidden TOAST table, storing only a pointer in the main table.' },
    { id: 'pg-020', question: 'What is a BRIN index?', difficulty: 'expert', type: 'theory', answer: 'Block Range INdex. Used for massive tables where the data is naturally ordered (like timestamps). It stores the min/max values for ranges of data blocks. Incredibly small and fast to build compared to B-Trees, but only effective on highly correlated/sequential data.' },
    { id: 'pg-021', question: 'What is the `ON CONFLICT` clause?', difficulty: 'medium', type: 'practical', answer: 'Also known as "Upsert". Used with `INSERT`. If the insertion violates a unique constraint, you can instruct Postgres to either `DO NOTHING` or `DO UPDATE SET ...`. `INSERT INTO users (id, name) VALUES (1, \'A\') ON CONFLICT (id) DO UPDATE SET name = EXCLUDED.name;`' },
    { id: 'pg-022', question: 'What is an Array type in Postgres?', difficulty: 'medium', type: 'practical', answer: 'Postgres natively supports arrays of any built-in or user-defined type. You can query them using operators like `ANY` (e.g., `WHERE \'tag\' = ANY(tags_array)`), `@>` (contains), and `<@` (is contained by). Can be indexed with GIN.' },
    { id: 'pg-023', question: 'Explain Logical vs Streaming Replication.', difficulty: 'hard', type: 'theory', answer: 'Streaming (Physical) Replication: Standby server copies byte-for-byte disk changes via WAL. Exactly mirrors the master, read-only. Logical Replication: Decodes WAL into logical data changes (INSERT/UPDATE). Allows replicating specific tables, filtering data, and writing to the subscriber DB.' },
    { id: 'pg-024', question: 'What is a Materialized View?', difficulty: 'medium', type: 'practical', answer: 'Caches the result of a complex query physically on disk. Great for dashboards. Must be manually updated using `REFRESH MATERIALIZED VIEW`. `CONCURRENTLY` can be used to refresh it without locking out readers, provided it has a unique index.' },
    { id: 'pg-025', question: 'What is `pg_dump` and `pg_restore`?', difficulty: 'easy', type: 'practical', answer: 'Command-line tools. `pg_dump` backs up a Postgres database into a script or archive file. `pg_restore` restores a Postgres database from an archive created by pg_dump.' },
    { id: 'pg-026', question: 'What is the `HSTORE` data type?', difficulty: 'medium', type: 'theory', answer: 'A key-value store module within Postgres. Stores sets of key-value pairs within a single PostgreSQL value. Mostly superseded by `JSONB`, but still used in some legacy systems for simple flat dictionaries.' },
    { id: 'pg-027', question: 'How do you find the size of a database or table?', difficulty: 'easy', type: 'practical', answer: 'Database: `SELECT pg_size_pretty(pg_database_size(\'dbname\'));` Table (including indexes): `SELECT pg_size_pretty(pg_total_relation_size(\'tablename\'));`.' },
    { id: 'pg-028', question: 'What is the `serial` type?', difficulty: 'easy', type: 'theory', answer: 'A pseudo-type used to define auto-incrementing integers (like AUTO_INCREMENT in MySQL). Under the hood, it creates an `integer` column and attaches a `SEQUENCE` object to it as its default value. Replaced by `GENERATED ALWAYS AS IDENTITY` in modern PG (SQL Standard).' },
    { id: 'pg-029', question: 'Explain `LATERAL` joins.', difficulty: 'hard', type: 'practical', answer: 'A `JOIN LATERAL` allows a subquery in the FROM clause to reference columns from preceding tables in the FROM clause. It essentially acts as a foreach loop, running the subquery once for each row of the preceding table. Great for "top N per category" queries.' },
    { id: 'pg-030', question: 'What is the PostGIS extension?', difficulty: 'medium', type: 'theory', answer: 'The industry-standard spatial database extender for PostgreSQL. It adds support for geographic objects allowing location queries (e.g., finding all points within a certain radius, calculating distances, intersecting polygons) using spatial indexes (GiST).' },
    { id: 'pg-031', question: 'What is a Sequence?', difficulty: 'easy', type: 'theory', answer: 'A special single-row table used to generate sequential numbers. They are safe for concurrent usage. Used primarily for auto-incrementing primary keys. Accessed via functions like `nextval()`, `currval()`, and `setval()`.' },
    { id: 'pg-032', question: 'What are constraints in PostgreSQL?', difficulty: 'easy', type: 'theory', answer: 'Rules enforced on data columns. Includes: PRIMARY KEY, FOREIGN KEY, UNIQUE, NOT NULL, CHECK (ensures a condition is met, e.g., `CHECK (price > 0)`), and EXCLUSION (ensures no two rows overlap, often used with time ranges).' },
    { id: 'pg-033', question: 'What is the difference between TRUNCATE and DELETE?', difficulty: 'medium', type: 'theory', answer: 'DELETE scans the table row by row and writes each deletion to the WAL (slow). TRUNCATE quickly removes all rows by deallocating the data pages completely (fast). TRUNCATE resets sequences and cannot trigger DELETE triggers.' },
    { id: 'pg-034', question: 'What is `tempdb` equivalent in Postgres?', difficulty: 'medium', type: 'theory', answer: 'PostgreSQL does not have a global `tempdb`. Temporary tables are created in the current session\'s temporary schema (e.g., `pg_temp`). They are automatically dropped at the end of the session or transaction.' },
    { id: 'pg-035', question: 'How do you kill a blocking query?', difficulty: 'medium', type: 'practical', answer: 'Identify the PID using `pg_stat_activity`. Attempt to cancel the query gracefully using `SELECT pg_cancel_backend(pid);`. If it refuses to die, forcibly terminate the connection using `SELECT pg_terminate_backend(pid);`.' },
    { id: 'pg-036', question: 'What is the UUID data type?', difficulty: 'easy', type: 'practical', answer: 'A 128-bit quantity generated by an algorithm designed to make it unique across space and time. Postgres has native support for storing UUIDs (requires 16 bytes vs 36 bytes for text). You often use the `uuid-ossp` or `pgcrypto` extensions to generate them via `gen_random_uuid()`.' },
    { id: 'pg-037', question: 'Explain the role of `shared_buffers`.', difficulty: 'hard', type: 'theory', answer: 'The primary memory configuration parameter in postgresql.conf. It determines how much RAM Postgres uses to cache data blocks directly. Standard recommendation is to set it to 25% of total system RAM (leaving the rest for the OS filesystem cache).' },
    { id: 'pg-038', question: 'What is `work_mem`?', difficulty: 'hard', type: 'theory', answer: 'The amount of memory used by internal sort operations (like ORDER BY, DISTINCT) and hash tables (Hash Joins) BEFORE writing to temporary disk files. Increasing this speeds up complex queries, but it is applied PER OPERATION, meaning too high a value can crash the server under high concurrency.' },
    { id: 'pg-039', question: 'Explain Two-Phase Commit in Postgres.', difficulty: 'hard', type: 'theory', answer: 'Supported via `PREPARE TRANSACTION` and `COMMIT PREPARED`. Allows transactions to be split into two phases to guarantee distributed ACID transactions across multiple independent databases.' },
    { id: 'pg-040', question: 'What are table spaces (Tablespaces)?', difficulty: 'medium', type: 'theory', answer: 'Allow database administrators to define locations in the file system where the files representing database objects can be stored. Useful for putting heavily accessed tables on fast SSDs and archive tables on slower HDDs.' },
    { id: 'pg-041', question: 'What is an Advisory Lock?', difficulty: 'hard', type: 'practical', answer: 'Application-level locks created within Postgres. Unlike table or row locks, their meaning is defined entirely by the application. E.g., `pg_advisory_lock(123)` can be used to prevent a cron job from running concurrently on multiple application servers.' },
    { id: 'pg-042', question: 'What is the `LISTEN` and `NOTIFY` mechanism?', difficulty: 'hard', type: 'practical', answer: 'PostgreSQL\'s built-in lightweight publish/subscribe system. Clients can `LISTEN` to a channel. A database trigger or application can `NOTIFY channel, \'payload\'`, and the DB pushes the event to all listening clients instantly.' },
    { id: 'pg-043', question: 'Explain Row-Level Security (RLS).', difficulty: 'hard', type: 'practical', answer: 'Allows database administrators to set policies that restrict which rows are returned by normal queries or which rows can be inserted/updated/deleted, based on the user executing the query. Perfect for multi-tenant SaaS applications.' },
    { id: 'pg-044', question: 'What is the `COPY` command?', difficulty: 'medium', type: 'practical', answer: 'A Postgres-specific command that moves data between tables and standard file-system files (like CSV). `COPY` is the fastest possible way to bulk insert data into Postgres.' },
    { id: 'pg-045', question: 'What is a GiST index?', difficulty: 'expert', type: 'theory', answer: 'Generalized Search Tree. A template for building custom index structures. Used heavily for 2D spatial data (PostGIS bounding boxes), text search, and ranges where B-Trees are insufficient.' },
    { id: 'pg-046', question: 'How do you handle JSON array querying?', difficulty: 'medium', type: 'practical', answer: 'Using specific operators. E.g., `jsonb_column @> \'[{"id": 2}]\'` checks if the JSON array contains the exact object. You can also use `jsonb_array_elements()` to expand the array into a set of rows.' },
    { id: 'pg-047', question: 'What is the `pg_stat_statements` extension?', difficulty: 'hard', type: 'practical', answer: 'An essential extension that records execution statistics of all SQL statements executed. It provides data on total execution time, min/max times, calls, and blocks hit/read, allowing you to identify the slowest queries in production.' },
    { id: 'pg-048', question: 'What is `pg_hba.conf`?', difficulty: 'easy', type: 'theory', answer: 'Host-Based Authentication configuration file. Controls which hosts (IPs/subnets) are allowed to connect, which databases they can access, which users they can log in as, and which authentication method (MD5, SCRAM, trust) must be used.' },
    { id: 'pg-049', question: 'What is the difference between `NULLIF` and `COALESCE`?', difficulty: 'easy', type: 'practical', answer: '`COALESCE(a, b)` returns `a` if `a` is not null, otherwise `b`. `NULLIF(a, b)` returns NULL if `a == b`, otherwise it returns `a`. Often used to prevent divide-by-zero errors: `val / NULLIF(divisor, 0)`.' },
    { id: 'pg-050', question: 'Explain the role of the Background Writer.', difficulty: 'expert', type: 'theory', answer: 'A background process that periodically writes dirty (modified) buffers from shared memory to disk. This reduces the number of writes required during checkpoints or when a query needs to evict a buffer, improving query latency.' }
  ]
};
