export const springBootData = {
  id: 'spring-boot',
  title: 'Spring Boot',
  icon: '🍃',
  summary: 'Opinionated, convention-over-configuration Java framework for building enterprise applications and microservices.',
  concepts: ['IoC & DI', 'Auto-Configuration', 'Annotations', 'Spring MVC', 'JPA/Hibernate', 'Spring Security', 'Actuator', 'AOP'],
  
  learningLinks: [
    { title: 'Node.js Docs', url: 'https://nodejs.org/en/docs/' },
    { title: 'Spring Boot Guide', url: 'https://spring.io/guides' }
  ],
  questions: [
    { id: 'sb-001', question: 'What is the difference between Spring and Spring Boot?', difficulty: 'easy', type: 'theory', answer: 'Spring is the core framework providing Dependency Injection (DI) and Inversion of Control (IoC). It requires heavy manual XML or Java configuration. Spring Boot is an extension that provides "opinionated" auto-configuration, embedded servers (Tomcat), and starter POMs to get applications running instantly without boilerplate.' },
    { id: 'sb-002', question: 'Explain Inversion of Control (IoC) and Dependency Injection (DI).', difficulty: 'medium', type: 'theory', answer: 'IoC means the control of object creation is handed over from the programmer to the framework (the Spring Container). DI is the pattern used to implement IoC; instead of objects instantiating their dependencies, the Container injects them (via constructor, setter, or field injection).' },
    { id: 'sb-003', question: 'What does @SpringBootApplication do?', difficulty: 'medium', type: 'theory', answer: 'It is a convenience annotation that wraps three core annotations: 1. `@Configuration` (allows registering extra beans in the context). 2. `@EnableAutoConfiguration` (tells Spring Boot to auto-configure beans based on classpath dependencies). 3. `@ComponentScan` (scans for other components, configurations, and services in the current package and its sub-packages).' },
    { id: 'sb-004', question: 'What are Spring Boot Starter dependencies?', difficulty: 'easy', type: 'theory', answer: 'A set of convenient dependency descriptors you can include in your application. E.g., `spring-boot-starter-web` automatically pulls in Spring MVC, Jackson (for JSON), and embeds Tomcat, saving you from copying/pasting complex dependency trees.' },
    { id: 'sb-005', question: 'How does Spring Boot Auto-Configuration work?', difficulty: 'hard', type: 'theory', answer: 'It attempts to guess and configure beans you are likely to need based on your classpath. It uses `@Conditional` annotations (e.g., `@ConditionalOnClass`, `@ConditionalOnMissingBean`) inside configuration classes listed in `META-INF/spring.factories` (or `org.springframework.boot.autoconfigure.AutoConfiguration.imports` in newer versions).' },
    { id: 'sb-006', question: 'Constructor Injection vs Field Injection.', difficulty: 'medium', type: 'practical', answer: 'Constructor injection (passing dependencies via constructor) is the recommended best practice. It ensures the object is initialized in a complete state, makes the class easier to unit test without Spring, and allows dependencies to be `final`. Field injection (`@Autowired` on a field) is discouraged because it hides dependencies and makes testing harder.' },
    { id: 'sb-007', question: 'Difference between @Component, @Service, @Repository, and @Controller?', difficulty: 'easy', type: 'theory', answer: '`@Component` is a generic stereotype for any Spring-managed component. `@Service`, `@Repository`, and `@Controller` are specializations of `@Component` used for specific layers. `@Repository` provides automatic translation of database exceptions. `@Controller` marks a class as a web handler. `@Service` currently adds no special behavior but clarifies intent.' },
    { id: 'sb-008', question: 'What is the Bean lifecycle in Spring?', difficulty: 'hard', type: 'theory', answer: '1. Instantiation. 2. Populate Properties (DI). 3. BeanNameAware / BeanFactoryAware. 4. Pre-initialization (`BeanPostProcessor.postProcessBeforeInitialization`). 5. Initialization (`@PostConstruct`, `afterPropertiesSet`). 6. Post-initialization (`BeanPostProcessor.postProcessAfterInitialization`, where AOP proxies are created). 7. Ready for use. 8. Destruction (`@PreDestroy`, `destroy()`).' },
    { id: 'sb-009', question: 'What are the default bean scopes in Spring?', difficulty: 'medium', type: 'theory', answer: 'The default scope is `Singleton` (one instance per Spring IoC container). Other scopes: `Prototype` (new instance every time it is requested), `Request` (one per HTTP request), `Session` (one per HTTP session), and `Application` (one per ServletContext).' },
    { id: 'sb-010', question: 'Difference between @Controller and @RestController?', difficulty: 'easy', type: 'theory', answer: '`@Controller` is used for traditional web applications returning Views (HTML). You must use `@ResponseBody` on methods to return data. `@RestController` is a convenience annotation that combines `@Controller` and `@ResponseBody`, meaning every method returns data (JSON/XML) directly to the response body.' },
    { id: 'sb-011', question: 'How do you handle exceptions globally in Spring Boot?', difficulty: 'medium', type: 'practical', answer: 'Use the `@ControllerAdvice` or `@RestControllerAdvice` annotation on a class. Inside, use `@ExceptionHandler(SpecificException.class)` on methods to catch unhandled exceptions thrown by any controller and return a standardized error response (e.g., a custom ErrorDTO and HTTP status).' },
    { id: 'sb-012', question: 'What is Spring Data JPA?', difficulty: 'medium', type: 'theory', answer: 'It is a layer on top of JPA (Java Persistence API) providers like Hibernate. It significantly reduces boilerplate by allowing you to define repository interfaces (extending `JpaRepository`). Spring automatically generates the implementation and common CRUD methods at runtime.' },
    { id: 'sb-013', question: 'Difference between CrudRepository and JpaRepository?', difficulty: 'easy', type: 'theory', answer: '`CrudRepository` provides basic CRUD operations. `JpaRepository` extends `PagingAndSortingRepository` (which extends `CrudRepository`) and adds JPA-specific methods like flushing the persistence context (`flush()`) and batch deletion (`deleteInBatch()`).' },
    { id: 'sb-014', question: 'What is the @Transactional annotation?', difficulty: 'medium', type: 'practical', answer: 'It defines the scope of a single database transaction. If an unchecked exception (RuntimeException) is thrown within the annotated method, the transaction automatically rolls back. If it finishes successfully, it commits. Can be applied to classes or methods.' },
    { id: 'sb-015', question: 'What are Transaction Propagation levels?', difficulty: 'hard', type: 'theory', answer: 'Define how transactions relate to each other when methods call other transactional methods. Default is `REQUIRED` (use existing, or create new if none exists). Others: `REQUIRES_NEW` (always create a new one, suspending the current), `SUPPORTS` (use if exists, else run non-transactional), `MANDATORY` (throw exception if no transaction exists).' },
    { id: 'sb-016', question: 'What is Spring Security?', difficulty: 'medium', type: 'theory', answer: 'A powerful and highly customizable authentication and access-control framework. It intercepts requests via a chain of Servlet Filters to handle authentication (who are you?) and authorization (are you allowed to do this?).' },
    { id: 'sb-017', question: 'Explain the Filter Chain in Spring Security.', difficulty: 'hard', type: 'theory', answer: 'A sequence of filters managed by `FilterChainProxy`. Includes filters like `UsernamePasswordAuthenticationFilter` (processes logins), `BasicAuthenticationFilter` (processes HTTP Basic auth), and `FilterSecurityInterceptor` (checks authorization rules at the end of the chain before allowing access to the Controller).' },
    { id: 'sb-018', question: 'How do you secure a REST API with JWT in Spring Boot?', difficulty: 'hard', type: 'practical', answer: '1. Create a custom authentication filter that extends `OncePerRequestFilter`. 2. Extract the JWT from the Authorization header. 3. Validate the token signature using a library (e.g., jjwt). 4. Load the user authorities and place them in the `SecurityContextHolder`. 5. Register the filter in the `SecurityFilterChain`.' },
    { id: 'sb-019', question: 'What is Spring Boot Actuator?', difficulty: 'medium', type: 'theory', answer: 'A module providing production-ready features. It exposes HTTP endpoints (e.g., `/actuator/health`, `/actuator/metrics`, `/actuator/info`) to monitor and manage your application, view loaded beans, environment variables, and thread dumps.' },
    { id: 'sb-020', question: 'What is AOP (Aspect-Oriented Programming) in Spring?', difficulty: 'hard', type: 'theory', answer: 'A programming paradigm that aims to increase modularity by allowing the separation of cross-cutting concerns (like logging, security, transactions). It uses "Aspects" containing "Advice" (code to run) applied at "Pointcuts" (where to run it, like before specific methods).' },
    { id: 'sb-021', question: 'Explain @Primary vs @Qualifier.', difficulty: 'medium', type: 'practical', answer: 'When multiple beans of the same type exist, DI will fail with a `NoUniqueBeanDefinitionException`. `@Primary` on a bean makes it the default choice. `@Qualifier("beanName")` is used at the injection point to explicitly specify WHICH bean by name should be injected.' },
    { id: 'sb-022', question: 'What is the purpose of application.properties / application.yml?', difficulty: 'easy', type: 'theory', answer: 'Files used to configure Spring Boot applications. Used for defining server ports, database connection details, logging levels, custom properties, and overriding default auto-configuration values.' },
    { id: 'sb-023', question: 'How do you manage database schema changes in Spring Boot?', difficulty: 'medium', type: 'practical', answer: 'Do not rely on Hibernate\'s `hbm2ddl.auto=update` in production. Use migration tools like Flyway or Liquibase. You place SQL scripts in `src/main/resources/db/migration`, and Spring Boot automatically executes them on startup to keep the schema versioned and synchronized.' },
    { id: 'sb-024', question: 'What is the N+1 select problem in Hibernate and how to fix it?', difficulty: 'hard', type: 'practical', answer: 'Occurs when you fetch a list of entities (1 query) and then access a lazily loaded collection on each entity, resulting in N additional queries. Fixes: 1. Use `JOIN FETCH` in JPQL/HQL. 2. Use `@EntityGraph`. 3. Use `@BatchSize`.' },
    { id: 'sb-025', question: 'Explain Spring Profiles.', difficulty: 'medium', type: 'practical', answer: 'Profiles provide a way to segregate parts of your application configuration and make it available only in certain environments (e.g., `dev`, `test`, `prod`). Activated via `spring.profiles.active`. You can have environment-specific files like `application-prod.yml`.' },
    { id: 'sb-026', question: 'Difference between @RequestParam and @PathVariable?', difficulty: 'easy', type: 'theory', answer: '`@PathVariable` extracts values from the URI path (e.g., `/users/{id}`). `@RequestParam` extracts values from the URI query string (e.g., `/users?id=123`).' },
    { id: 'sb-027', question: 'How does Spring handle validation?', difficulty: 'medium', type: 'practical', answer: 'Using the Hibernate Validator (JSR 380). You annotate your DTO fields with rules like `@NotNull`, `@Size`, `@Email`. In the controller, you add the `@Valid` annotation to the `@RequestBody` parameter. If validation fails, a `MethodArgumentNotValidException` is thrown.' },
    { id: 'sb-028', question: 'What is the purpose of @Value?', difficulty: 'easy', type: 'practical', answer: 'Used to inject values from properties files into fields. Example: `@Value("${server.port}") private int port;`. Can also use SpEL (Spring Expression Language).' },
    { id: 'sb-029', question: 'Explain @ConfigurationProperties.', difficulty: 'medium', type: 'practical', answer: 'A strongly typed way to bind external configuration (properties/YAML) to Java objects. Unlike `@Value`, it binds an entire prefix (e.g., `app.mail.*`) to a POJO with matching fields. It supports validation and generates metadata for IDE autocompletion.' },
    { id: 'sb-030', question: 'What is the role of the DispatcherServlet?', difficulty: 'hard', type: 'theory', answer: 'The heart of Spring MVC. It acts as the Front Controller, intercepting all incoming HTTP requests. It consults the `HandlerMapping` to find the appropriate Controller, invokes it, takes the returned ModelAndView, resolves the view (or serializes to JSON via message converters), and returns the response.' },
    { id: 'sb-031', question: 'How do you schedule tasks in Spring Boot?', difficulty: 'medium', type: 'practical', answer: '1. Add `@EnableScheduling` to a configuration class. 2. Add `@Scheduled` to a method (must return void and take no arguments). You can specify a `fixedRate`, `fixedDelay`, or a `cron` expression.' },
    { id: 'sb-032', question: 'What is the Spring Context (ApplicationContext)?', difficulty: 'medium', type: 'theory', answer: 'The advanced Spring IoC container. It is responsible for instantiating, configuring, and assembling beans. It extends `BeanFactory` with extra features like event publishing, internationalization, and AOP.' },
    { id: 'sb-033', question: 'How to do asynchronous processing in Spring Boot?', difficulty: 'medium', type: 'practical', answer: '1. Add `@EnableAsync` to a configuration class. 2. Annotate a method with `@Async`. Spring will execute this method in a separate thread from a thread pool. The method can return `void` or a `CompletableFuture`.' },
    { id: 'sb-034', question: 'What are Spring Boot Starters?', difficulty: 'easy', type: 'theory', answer: 'Dependency descriptors that aggregate common dependencies into a single package. Examples: `spring-boot-starter-web` (REST, Tomcat, Jackson), `spring-boot-starter-data-jpa` (Hibernate, Spring Data).' },
    { id: 'sb-035', question: 'Explain the difference between @Bean and @Component.', difficulty: 'medium', type: 'theory', answer: '`@Component` is a class-level annotation where Spring automatically detects and registers the class as a bean (via component scanning). `@Bean` is a method-level annotation (usually inside a `@Configuration` class) where you manually instantiate and configure the bean before returning it to the container. Used heavily for third-party classes.' },
    { id: 'sb-036', question: 'What is Hibernate?', difficulty: 'easy', type: 'theory', answer: 'An Object-Relational Mapping (ORM) framework for Java. It maps Java classes to database tables and translates Java data types to SQL data types, saving developers from writing raw SQL.' },
    { id: 'sb-037', question: 'What is the L1 and L2 cache in Hibernate?', difficulty: 'hard', type: 'theory', answer: 'L1 Cache: Session-level cache, enabled by default. Exists only for the duration of the transaction/session. L2 Cache: SessionFactory-level cache, disabled by default. Shared across all sessions. Requires a third-party provider like Ehcache.' },
    { id: 'sb-038', question: 'How do you test a Spring Boot application?', difficulty: 'medium', type: 'practical', answer: 'Use `@SpringBootTest` for integration testing (loads the entire application context). Use `@WebMvcTest` to test only the web layer (MockMvc). Use `@DataJpaTest` to test repositories with an in-memory database like H2. Use `@MockBean` to mock dependencies.' },
    { id: 'sb-039', question: 'What is @MockBean vs @Mock?', difficulty: 'medium', type: 'theory', answer: '`@Mock` (Mockito) creates a standalone mock object. `@MockBean` (Spring Boot Test) creates a mock object AND replaces the existing bean of the same type in the Spring ApplicationContext. Essential for integration testing.' },
    { id: 'sb-040', question: 'What is CORS and how to enable it in Spring Boot?', difficulty: 'medium', type: 'practical', answer: 'Cross-Origin Resource Sharing. Enable it locally using the `@CrossOrigin` annotation on a Controller class or method. Enable it globally by implementing `WebMvcConfigurer` and overriding `addCorsMappings()`.' },
    { id: 'sb-041', question: 'Explain standard vs mapped superclass in JPA.', difficulty: 'hard', type: 'theory', answer: '`@MappedSuperclass` defines common fields (like `createdAt`, `updatedAt`) for subclasses to inherit. The superclass itself is NOT an entity and has no table. If it were a regular `@Entity` and inherited, JPA would use inheritance strategies like Single Table or Joined.' },
    { id: 'sb-042', question: 'What are Entity callbacks in JPA?', difficulty: 'medium', type: 'theory', answer: 'Methods annotated with `@PrePersist`, `@PostPersist`, `@PreUpdate`, etc. They execute automatically at specific stages of the entity lifecycle. Useful for setting audit fields (e.g., `updatedAt = new Date()`).' },
    { id: 'sb-043', question: 'Difference between @Query and method naming conventions in Spring Data?', difficulty: 'easy', type: 'theory', answer: 'Method names (e.g., `findByLastNameAndAge`) auto-generate SQL queries. `@Query` allows writing custom JPQL or native SQL (`nativeQuery = true`) directly on the repository interface for complex operations.' },
    { id: 'sb-044', question: 'What is a DTO and why use it?', difficulty: 'easy', type: 'theory', answer: 'Data Transfer Object. An object used to encapsulate data and send it from one subsystem to another. Prevents exposing internal domain models (Entities) directly to the API client, avoiding security risks and over-fetching.' },
    { id: 'sb-045', question: 'How to handle file uploads in Spring Boot?', difficulty: 'medium', type: 'practical', answer: 'Use `MultipartFile` as a parameter in your `@PostMapping`. Ensure your form specifies `enctype="multipart/form-data"`. Spring automatically parses the multipart request.' },
    { id: 'sb-046', question: 'What is Spring WebFlux?', difficulty: 'hard', type: 'theory', answer: 'The reactive-stack web framework in Spring. Non-blocking, asynchronous, and event-driven. Runs on Netty instead of Tomcat. Uses Project Reactor (`Mono` for 0-1 items, `Flux` for 0-N items). Best for high-concurrency systems.' },
    { id: 'sb-047', question: 'What is the purpose of @ControllerAdvice?', difficulty: 'medium', type: 'theory', answer: 'A specialization of `@Component` that allows you to consolidate multiple scattered `@ExceptionHandler` methods into a single, global error handling component applied to all Controllers.' },
    { id: 'sb-048', question: 'How do you externalize configuration in Spring Boot?', difficulty: 'medium', type: 'practical', answer: 'Using `application.properties`, environment variables, command-line arguments, or Spring Cloud Config server. Spring evaluates them in a specific order (e.g., command-line args override env vars, which override properties files).' },
    { id: 'sb-049', question: 'What is the `RestTemplate` vs `WebClient`?', difficulty: 'medium', type: 'theory', answer: '`RestTemplate` is the traditional synchronous HTTP client in Spring. `WebClient` (from WebFlux) is the modern, non-blocking, reactive HTTP client. Spring explicitly recommends `WebClient` for new developments.' },
    { id: 'sb-050', question: 'What is Jackson in Spring Boot?', difficulty: 'easy', type: 'theory', answer: 'The default JSON processing library. It automatically serializes Java objects returned by `@RestController` into JSON, and deserializes JSON from `@RequestBody` into Java objects. Configurable via annotations like `@JsonIgnore` or `@JsonProperty`.' }
  ]
};
