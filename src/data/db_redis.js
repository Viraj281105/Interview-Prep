export const redisData = {
  id: 'redis',
  title: 'Redis Advanced Patterns',
  icon: '⚡',
  summary: 'In-memory data structure store, used as a database, cache, and message broker.',
  concepts: ['Data Types', 'Persistence (RDB/AOF)', 'Pub/Sub', 'Eviction Policies', 'Transactions', 'Lua Scripting', 'Clustering', 'Pipelining'],
  questions: [
    { id: 'red-001', question: 'What is Redis?', difficulty: 'easy', type: 'theory', answer: 'Remote Dictionary Server. An open-source, in-memory, key-value data structure store. It is extremely fast (sub-millisecond latency) and can be used as a database, cache, message broker, and streaming engine.' },
    { id: 'red-002', question: 'Why is Redis so fast?', difficulty: 'medium', type: 'theory', answer: '1. In-Memory: Data resides in RAM, bypassing slow disk I/O. 2. Single-Threaded Event Loop (mostly): Avoids context switching and lock overhead (though I/O and background tasks use threads). 3. Highly optimized C codebase. 4. Efficient data structures (e.g., skip lists for Sorted Sets).' },
    { id: 'red-003', question: 'Is Redis entirely single-threaded?', difficulty: 'hard', type: 'theory', answer: 'Historically yes, the core command execution is strictly single-threaded. However, modern Redis (v6+) uses multi-threading for network I/O (reading requests/writing responses), while the actual command execution remains single-threaded to guarantee atomicity and avoid locks.' },
    { id: 'red-004', question: 'What are the main Data Types in Redis?', difficulty: 'medium', type: 'theory', answer: 'Strings, Lists, Sets, Hashes, Sorted Sets (ZSet), Bitmaps, HyperLogLogs, Geospatial indexes, and Streams.' },
    { id: 'red-005', question: 'Explain Redis Hashes.', difficulty: 'easy', type: 'practical', answer: 'A map between a string field and a string value (like a flat JSON object). Great for storing objects. Commands: `HSET user:100 name "John" age 30`, `HGET user:100 name`, `HGETALL user:100`.' },
    { id: 'red-006', question: 'Explain Redis Lists and their use cases.', difficulty: 'medium', type: 'practical', answer: 'Collections of string elements sorted according to the order of insertion (implemented as Linked Lists). Commands: `LPUSH`, `RPUSH`, `LPOP`, `RPOP`. Use cases: Activity streams, recent updates, simple message queues.' },
    { id: 'red-007', question: 'Explain Redis Sets.', difficulty: 'medium', type: 'practical', answer: 'Unordered collections of unique strings. Commands: `SADD`, `SREM`, `SISMEMBER` (check existence in O(1)). Supports fast set operations: Intersections (`SINTER`), Unions (`SUNION`), Differences (`SDIFF`). Use cases: Unique tags, mutual friends.' },
    { id: 'red-008', question: 'Explain Redis Sorted Sets (ZSets).', difficulty: 'hard', type: 'practical', answer: 'Like Sets, but every element is associated with a floating-point score. Elements are ordered by this score. Commands: `ZADD`, `ZRANGE`, `ZREVRANK`. Under the hood, uses a Hash + Skip List. Use cases: Leaderboards, priority queues, rate limiters.' },
    { id: 'red-009', question: 'How do you implement a Leaderboard in Redis?', difficulty: 'medium', type: 'practical', answer: 'Use a Sorted Set (ZSet). `ZADD leaderboard 100 "UserA"`. To get the top 10: `ZREVRANGE leaderboard 0 9 WITHSCORES`. To get a user\'s rank: `ZREVRANK leaderboard "UserA"`.' },
    { id: 'red-010', question: 'What is a HyperLogLog?', difficulty: 'expert', type: 'theory', answer: 'A probabilistic data structure used to estimate the cardinality (number of unique elements) of a massive dataset. It uses only 12KB of memory and has a standard error of 0.81%. Great for counting unique daily active users (DAU) without storing every IP/ID. Commands: `PFADD`, `PFCOUNT`.' },
    { id: 'red-011', question: 'What are Bitmaps?', difficulty: 'hard', type: 'practical', answer: 'Not a separate data type, but bit-level operations performed on Strings. You can set individual bits to 0 or 1. extremely space-efficient. Commands: `SETBIT`, `GETBIT`, `BITCOUNT`. Use cases: Tracking user daily logins (1 bit per day per user).' },
    { id: 'red-012', question: 'Explain Redis Persistence (RDB vs AOF).', difficulty: 'hard', type: 'theory', answer: 'Since RAM is volatile, Redis offers persistence. RDB (Redis Database Backup): Point-in-time snapshots of the dataset saved to disk at specified intervals. AOF (Append Only File): Logs every write operation received. Slower but more durable.' },
    { id: 'red-013', question: 'Pros and Cons of RDB.', difficulty: 'medium', type: 'theory', answer: 'Pros: Compact file (great for backups), fast restarts, minimizes performance impact (Redis forks a child process to save). Cons: You lose data written since the last snapshot if the server crashes (e.g., up to 5 minutes of data loss).' },
    { id: 'red-014', question: 'Pros and Cons of AOF.', difficulty: 'medium', type: 'theory', answer: 'Pros: High durability (can sync to disk every second or every command), log is append-only (no corruption on crash). Cons: Files grow much larger than RDB, slower restarts (must replay the log), slightly degrades write performance.' },
    { id: 'red-015', question: 'What is AOF Rewrite?', difficulty: 'hard', type: 'theory', answer: 'Because AOF logs every command, it gets huge (e.g., `SET x 1`, `SET x 2`, `SET x 3`). Redis periodically runs a background rewrite to create a new, minimal AOF file reflecting the current state (e.g., just `SET x 3`), discarding redundant commands.' },
    { id: 'red-016', question: 'What is Redis Pipelining?', difficulty: 'medium', type: 'practical', answer: 'A technique to improve performance by sending multiple commands to the server in a single batch, without waiting for the replies of the previous commands. It drastically reduces network latency (round-trip time) for bulk operations.' },
    { id: 'red-017', question: 'What are Redis Transactions?', difficulty: 'medium', type: 'practical', answer: 'Allows executing a group of commands sequentially. Starts with `MULTI`, followed by commands (which are queued), and executed with `EXEC` (or aborted with `DISCARD`). Unlike relational DBs, there is NO ROLLBACK if a command fails during execution.' },
    { id: 'red-018', question: 'What is the `WATCH` command in Redis?', difficulty: 'hard', type: 'practical', answer: 'Provides optimistic locking for transactions. You `WATCH key`. If another client modifies that key before your `EXEC`, your transaction fails and returns null. You can then retry.' },
    { id: 'red-019', question: 'Explain Redis Pub/Sub.', difficulty: 'medium', type: 'theory', answer: 'A messaging paradigm. Publishers send messages to `channels`. Subscribers listen to `channels`. It is an "at-most-once" delivery system: if a subscriber is offline when a message is published, they never get it. Used for real-time notifications/chat.' },
    { id: 'red-020', question: 'What are Redis Streams?', difficulty: 'hard', type: 'theory', answer: 'Introduced in v5, it\'s an append-only log data structure similar to Kafka. Overcomes Pub/Sub limitations: messages are persisted, clients can read historical messages, and Consumer Groups allow multiple clients to share the processing of messages.' },
    { id: 'red-021', question: 'How do you expire keys in Redis?', difficulty: 'easy', type: 'practical', answer: 'Use the `EXPIRE key seconds` command to set a time-to-live. Or set it upon creation: `SET key value EX 60`. `TTL key` returns remaining time.' },
    { id: 'red-022', question: 'How does Redis delete expired keys?', difficulty: 'hard', type: 'theory', answer: 'Two ways. Passive: When a client tries to access a key, Redis checks the TTL; if expired, it deletes and returns null. Active: Redis periodically tests a random sample of keys with TTLs and deletes expired ones in the background to free memory.' },
    { id: 'red-023', question: 'What happens when Redis runs out of memory?', difficulty: 'medium', type: 'theory', answer: 'It depends on the `maxmemory-policy` configuration. If set to `noeviction` (default), it will return errors for write commands. Otherwise, it will evict (delete) keys to make room based on algorithms like LRU, LFU, or random.' },
    { id: 'red-024', question: 'Explain LRU vs LFU Eviction Policies.', difficulty: 'hard', type: 'theory', answer: 'LRU (Least Recently Used): Evicts keys that have not been accessed for the longest time. LFU (Least Frequently Used): Evicts keys that are accessed the least often, preserving keys that are highly popular even if they haven\'t been accessed in the last few seconds.' },
    { id: 'red-025', question: 'What is Lua Scripting in Redis?', difficulty: 'hard', type: 'practical', answer: 'You can write scripts in the Lua language and execute them on the Redis server using `EVAL`. Scripts execute atomically (blocking other commands), eliminating race conditions and reducing network round-trips for complex logic (e.g., checking a balance and deducting in one step).' },
    { id: 'red-026', question: 'What is Redis Sentinel?', difficulty: 'hard', type: 'theory', answer: 'Provides High Availability. A distributed system of Sentinel processes that monitor Master and Replica nodes. If the Master fails, Sentinels automatically agree on a failure, promote a Replica to Master, and notify clients of the new address.' },
    { id: 'red-027', question: 'What is Redis Cluster?', difficulty: 'expert', type: 'theory', answer: 'Provides horizontal scaling (sharding). Data is automatically partitioned across multiple Redis nodes. Uses algorithmic sharding (16384 Hash Slots) instead of consistent hashing. If a node fails, its replica takes over its slots.' },
    { id: 'red-028', question: 'What is a Hash Slot in Redis Cluster?', difficulty: 'expert', type: 'theory', answer: 'There are exactly 16384 hash slots. Every key is hashed (`CRC16(key) mod 16384`) to determine its slot. Each master node in the cluster is responsible for a subset of these slots. This makes adding/removing nodes easy by migrating slots.' },
    { id: 'red-029', question: 'What are Hash Tags in Redis Cluster?', difficulty: 'expert', type: 'practical', answer: 'If a key contains `{...}`, only the substring inside the braces is hashed. E.g., `user:{100}:profile` and `user:{100}:stats` will hash to the same slot, guaranteeing they live on the same physical node, allowing multi-key operations (like transactions) on them.' },
    { id: 'red-030', question: 'What is the Cache Stampede (Thundering Herd) problem?', difficulty: 'hard', type: 'theory', answer: 'When a highly popular cached item expires, hundreds of concurrent requests miss the cache simultaneously and hit the backend database to regenerate it, potentially crashing the DB. Mitigated by early recalculation, mutex locks, or jitter.' },
    { id: 'red-031', question: 'What is Cache Penetration?', difficulty: 'hard', type: 'theory', answer: 'When attackers request keys that do NOT exist in the cache OR the database. The cache misses, the DB is queried, and the DB returns null. Repeat 10,000 times, the DB crashes. Mitigation: Cache null values with short TTLs, or use a Bloom Filter before querying.' },
    { id: 'red-032', question: 'What is a Bloom Filter?', difficulty: 'expert', type: 'theory', answer: 'A highly space-efficient probabilistic data structure. It tells you if an item is "definitely not in the set" or "possibly in the set". Used in front of caches/databases to instantly drop requests for non-existent keys (preventing cache penetration). Available in Redis via RedisBloom.' },
    { id: 'red-033', question: 'What is Cache Avalanche?', difficulty: 'hard', type: 'theory', answer: 'When a large number of cached keys expire at the exact same time (because they were set with the same TTL). This causes a massive spike in DB load. Mitigation: Add a random "jitter" to TTLs (e.g., `TTL = 60 mins + random(0-5) mins`).' },
    { id: 'red-034', question: 'Difference between Memcached and Redis?', difficulty: 'medium', type: 'theory', answer: 'Memcached: Purely an in-memory key-value cache. Strings only. No persistence. Multi-threaded. Redis: Advanced data structures (Lists, Sets, Hashes), persistence (RDB/AOF), Pub/Sub, Lua scripting, transactions. Single-threaded (historically).' },
    { id: 'red-035', question: 'What is the Redlock algorithm?', difficulty: 'expert', type: 'theory', answer: 'A distributed lock manager algorithm proposed by the creator of Redis. It attempts to acquire a lock in a majority of N independent Redis nodes to provide a safe distributed lock, handling node crashes and clock drifts. Hard to implement correctly.' },
    { id: 'red-036', question: 'How do you implement a Rate Limiter in Redis?', difficulty: 'hard', type: 'practical', answer: '1. Fixed Window: Increment a key `user:ip:minute_timestamp` and set TTL to 60s. 2. Sliding Window: Use a Sorted Set, `ZADD user:ip timestamp timestamp`, remove old timestamps (`ZREMRANGEBYSCORE`), and `ZCOUNT` the current size.' },
    { id: 'red-037', question: 'What is `KEYS *` and why is it dangerous in production?', difficulty: 'medium', type: 'practical', answer: 'It returns all keys matching a pattern. Because Redis is single-threaded, if you have millions of keys, it will block the server for seconds, causing all other requests to time out. Use `SCAN` instead.' },
    { id: 'red-038', question: 'What is the `SCAN` command?', difficulty: 'medium', type: 'practical', answer: 'A cursor-based iterator. It returns a small subset of keys and a cursor. You pass the cursor to the next `SCAN` call. It does not block the server, making it safe for production, though it might return duplicates or miss keys if they are added/deleted during iteration.' },
    { id: 'red-039', question: 'What is the Info command?', difficulty: 'easy', type: 'practical', answer: 'Returns information and statistics about the server in a format that is simple to parse by computers and easy to read by humans. Covers memory usage, connected clients, replication status, and CPU usage.' },
    { id: 'red-040', question: 'How do you handle Sessions using Redis?', difficulty: 'medium', type: 'practical', answer: 'Store the session ID in a client cookie. In Redis, set the key as `session:ID` with the user\'s data (JSON or Hash) and a TTL matching the session expiry. It is fast and naturally distributed across horizontally scaled application servers.' },
    { id: 'red-041', question: 'What is the `SETNX` command?', difficulty: 'medium', type: 'practical', answer: 'Set if Not eXists. Sets the key only if it does not already exist. Extremely useful for implementing simple distributed locks. (Note: modern practice uses `SET key value NX PX 30000`).' },
    { id: 'red-042', question: 'What is a Distributed Lock?', difficulty: 'hard', type: 'theory', answer: 'A lock that spans multiple independent processes/servers to ensure that only one node performs a critical operation at a time (e.g., processing a payment). Redis is often used for this via `SET NX PX` and Lua scripts.' },
    { id: 'red-043', question: 'What is the purpose of the `PING` command?', difficulty: 'easy', type: 'practical', answer: 'Used to test if a connection is still alive, or to measure latency. Returns `PONG` if successful.' },
    { id: 'red-044', question: 'How do you optimize memory usage in Redis?', difficulty: 'hard', type: 'practical', answer: '1. Use small, concise key names (`usr:1` instead of `user:id:1`). 2. Use Hashes to store objects instead of multiple string keys (Redis optimizes small hashes). 3. Use Bitmaps or HyperLogLogs for analytics. 4. Avoid storing large blobs.' },
    { id: 'red-045', question: 'What are Client-Side Caching / Tracking features?', difficulty: 'expert', type: 'theory', answer: 'Introduced in Redis 6. Redis tracks which keys a client has requested. When those keys change, Redis sends an invalidation message to the client, allowing the client to maintain a fast local (in-process) cache safely.' },
    { id: 'red-046', question: 'What is Geospatial indexing in Redis?', difficulty: 'hard', type: 'practical', answer: 'Commands like `GEOADD`, `GEODIST`, `GEORADIUS`. Under the hood, coordinates are encoded as 52-bit Geohashes and stored in a Sorted Set. Allows extremely fast queries like "find all users within 5km of these coordinates".' },
    { id: 'red-047', question: 'What is the SLOWLOG?', difficulty: 'medium', type: 'practical', answer: 'A system to log queries that exceeded a specified execution time (`slowlog-log-slower-than` in microseconds). Safe to use in production as it is kept entirely in memory (circular buffer) and does not incur disk I/O.' },
    { id: 'red-048', question: 'Explain Master-Replica asynchronous replication.', difficulty: 'hard', type: 'theory', answer: 'Master acknowledges the write to the client immediately, then asynchronously sends the command to replicas. Fast, but risks data loss: if the master crashes before sending to replicas, and a replica is promoted, the acknowledged write is lost.' },
    { id: 'red-049', question: 'What is the `WAIT` command?', difficulty: 'expert', type: 'practical', answer: 'Forces the client to block and wait until the previous write commands are successfully acknowledged by at least N replicas, or a timeout occurs. It brings strong consistency (synchronous replication) at the cost of latency.' },
    { id: 'red-050', question: 'What is BGSAVE?', difficulty: 'medium', type: 'practical', answer: 'Saves the DB in the background. Redis forks a child process. The child process saves the RDB file to disk while the parent continues to serve clients. Uses OS Copy-On-Write semantics to minimize memory overhead during the save.' }
  ]
};
