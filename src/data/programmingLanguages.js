export const programmingLanguagesData = [
  {
    id: 'python',
    title: 'Python',
    icon: '🐍',
    summary: 'High-level, dynamically-typed, interpreted language known for readability and extensive standard library. Dominant in AI/ML, data science, and backend development.',
    concepts: ['GIL', 'Decorators', 'Generators', 'List Comprehensions', 'Duck Typing', 'MRO', 'Context Managers', 'Metaclasses', '*args/**kwargs', 'Slots', 'Descriptors', 'Asyncio'],
    questions: [
      { id: 'py-001', question: 'What is the difference between a list and a tuple in Python?', difficulty: 'easy', answer: 'Lists are mutable (can be changed after creation) while tuples are immutable (cannot be changed). Lists use square brackets [], tuples use parentheses (). Tuples are hashable and can be used as dictionary keys. Tuples are faster than lists due to immutability and static memory allocation. Lists have more built-in methods (append, extend, remove, etc.). Tuples are preferred for heterogeneous data (like database records), lists for homogeneous collections.', code: '# List - mutable\nmy_list = [1, 2, 3]\nmy_list[0] = 10  # OK\nmy_list.append(4)  # OK\n\n# Tuple - immutable\nmy_tuple = (1, 2, 3)\n# my_tuple[0] = 10  # TypeError!\n\n# Tuple as dict key\nd = {(1, 2): "point"}  # OK\n# d = {[1, 2]: "point"}  # TypeError!', codeLanguage: 'python' },
      { id: 'py-002', question: 'Explain the Global Interpreter Lock (GIL) in Python.', difficulty: 'hard', answer: 'The GIL is a mutex in CPython that allows only one thread to execute Python bytecode at a time. It exists because CPython\'s memory management (reference counting) is not thread-safe. Implications: CPU-bound multi-threaded programs don\'t achieve true parallelism. I/O-bound programs still benefit from threading because the GIL is released during I/O operations. For CPU-bound parallelism, use multiprocessing module, C extensions, or alternative interpreters like Jython/PyPy. Python 3.13+ introduces free-threaded mode (PEP 703) that removes the GIL experimentally.', code: 'import multiprocessing\nimport threading\nimport time\n\ndef cpu_bound(n):\n    count = 0\n    for i in range(n):\n        count += i\n    return count\n\n# Threading - limited by GIL for CPU-bound\nt1 = threading.Thread(target=cpu_bound, args=(10**7,))\nt2 = threading.Thread(target=cpu_bound, args=(10**7,))\n\n# Multiprocessing - bypasses GIL\np1 = multiprocessing.Process(target=cpu_bound, args=(10**7,))\np2 = multiprocessing.Process(target=cpu_bound, args=(10**7,))', codeLanguage: 'python' },
      { id: 'py-003', question: 'What are Python decorators and how do they work?', difficulty: 'medium', answer: 'Decorators are functions that modify the behavior of other functions or classes without changing their source code. They take a function as input and return a modified function. Decorators use the @decorator syntax (syntactic sugar). They are commonly used for logging, authentication, caching, timing, and access control. Decorators can be stacked, and they can accept arguments using decorator factories (a function that returns a decorator).', code: 'import functools\nimport time\n\n# Basic decorator\ndef timer(func):\n    @functools.wraps(func)\n    def wrapper(*args, **kwargs):\n        start = time.perf_counter()\n        result = func(*args, **kwargs)\n        elapsed = time.perf_counter() - start\n        print(f"{func.__name__} took {elapsed:.4f}s")\n        return result\n    return wrapper\n\n# Decorator with arguments\ndef retry(max_attempts=3):\n    def decorator(func):\n        @functools.wraps(func)\n        def wrapper(*args, **kwargs):\n            for attempt in range(max_attempts):\n                try:\n                    return func(*args, **kwargs)\n                except Exception as e:\n                    if attempt == max_attempts - 1:\n                        raise\n        return wrapper\n    return decorator\n\n@timer\n@retry(max_attempts=3)\ndef fetch_data(url):\n    pass', codeLanguage: 'python' },
      { id: 'py-004', question: 'Explain generators and the yield keyword.', difficulty: 'medium', answer: 'Generators are functions that return an iterator using the yield keyword instead of return. They generate values lazily (on demand) rather than computing all values at once. Key benefits: Memory efficient (values computed one at a time), can represent infinite sequences, support send() and throw() for two-way communication. Generator expressions provide compact syntax similar to list comprehensions. Generators are single-use iterators — once exhausted, they cannot be restarted.', code: '# Generator function\ndef fibonacci(limit):\n    a, b = 0, 1\n    while a < limit:\n        yield a\n        a, b = b, a + b\n\n# Usage - lazy evaluation\nfor num in fibonacci(100):\n    print(num)\n\n# Generator expression\nsquares = (x**2 for x in range(1000000))  # No memory for all values\n\n# Generator with send()\ndef accumulator():\n    total = 0\n    while True:\n        value = yield total\n        if value is not None:\n            total += value\n\nacc = accumulator()\nnext(acc)        # Initialize: 0\nacc.send(10)     # 10\nacc.send(20)     # 30', codeLanguage: 'python' },
      { id: 'py-005', question: 'What is the difference between deepcopy and shallow copy?', difficulty: 'medium', answer: 'Shallow copy creates a new object but references the same nested objects. Deep copy creates a new object and recursively copies all nested objects. Assignment (=) creates a reference, not a copy. For immutable objects (int, str, tuple), there\'s no practical difference. Shallow copy is faster but changes to nested mutable objects affect both copies. Deep copy is slower but creates fully independent objects. Use copy.copy() for shallow and copy.deepcopy() for deep copies.', code: 'import copy\n\noriginal = [[1, 2], [3, 4]]\n\n# Reference (not a copy)\nref = original\nref[0][0] = 99  # Changes original too!\n\n# Shallow copy\nshallow = copy.copy(original)\nshallow[0][0] = 100  # Changes original[0][0] too!\nshallow.append([5, 6])  # Does NOT affect original\n\n# Deep copy\ndeep = copy.deepcopy(original)\ndeep[0][0] = 200  # Does NOT affect original', codeLanguage: 'python' },
      { id: 'py-006', question: 'What are *args and **kwargs in Python?', difficulty: 'easy', answer: '*args collects positional arguments into a tuple. **kwargs collects keyword arguments into a dictionary. They allow functions to accept variable numbers of arguments. The names args/kwargs are conventions — the * and ** operators are what matter. Order in function signature: regular params, *args, keyword-only params, **kwargs. Can also be used for unpacking: *list_var unpacks a list, **dict_var unpacks a dictionary.', code: 'def flexible(*args, **kwargs):\n    print(f"Positional: {args}")    # tuple\n    print(f"Keyword: {kwargs}")     # dict\n\nflexible(1, 2, 3, name="Viraj", role="Engineer")\n# Positional: (1, 2, 3)\n# Keyword: {\'name\': \'Viraj\', \'role\': \'Engineer\'}\n\n# Unpacking\ndef greet(first, last, age):\n    return f"{first} {last}, age {age}"\n\ndata = {"first": "Viraj", "last": "K", "age": 22}\nprint(greet(**data))  # Viraj K, age 22', codeLanguage: 'python' },
      { id: 'py-007', question: 'Explain Python\'s Method Resolution Order (MRO).', difficulty: 'hard', answer: 'MRO defines the order in which base classes are searched when looking up a method. Python uses the C3 linearization algorithm. It ensures: children come before parents, the order of parents in the class definition is preserved, and a common parent appears only once. You can inspect MRO using ClassName.__mro__ or ClassName.mro(). MRO is crucial in multiple inheritance to avoid the diamond problem. super() follows the MRO chain, not just the immediate parent.', code: 'class A:\n    def method(self):\n        print("A")\n\nclass B(A):\n    def method(self):\n        print("B")\n        super().method()\n\nclass C(A):\n    def method(self):\n        print("C")\n        super().method()\n\nclass D(B, C):\n    def method(self):\n        print("D")\n        super().method()\n\nprint(D.__mro__)\n# (<class D>, <class B>, <class C>, <class A>, <class object>)\n\nD().method()\n# D -> B -> C -> A (follows MRO, not just parent)', codeLanguage: 'python' },
      { id: 'py-008', question: 'What are context managers and the with statement?', difficulty: 'medium', answer: 'Context managers handle setup and teardown of resources automatically. The with statement ensures proper acquisition and release of resources (files, locks, connections). They implement __enter__ and __exit__ methods. The contextlib module provides @contextmanager decorator for creating context managers from generator functions. __exit__ receives exception info and can suppress exceptions by returning True. Common uses: file handling, database connections, locks, temporary state changes.', code: '# Class-based context manager\nclass DatabaseConnection:\n    def __enter__(self):\n        self.conn = create_connection()\n        return self.conn\n    \n    def __exit__(self, exc_type, exc_val, exc_tb):\n        self.conn.close()\n        return False  # Don\'t suppress exceptions\n\n# contextlib decorator\nfrom contextlib import contextmanager\n\n@contextmanager\ndef timer(label):\n    import time\n    start = time.time()\n    try:\n        yield\n    finally:\n        print(f"{label}: {time.time() - start:.3f}s")\n\nwith timer("Processing"):\n    heavy_computation()', codeLanguage: 'python' },
      { id: 'py-009', question: 'Explain the difference between __str__ and __repr__.', difficulty: 'easy', answer: '__repr__ returns an unambiguous string representation for developers/debugging. __str__ returns a readable string for end users. If __str__ is not defined, Python falls back to __repr__. repr() calls __repr__, str() and print() call __str__. Convention: __repr__ should ideally return a string that could recreate the object (e.g., "ClassName(arg1, arg2)"). __str__ should return a human-friendly description.', code: 'class Point:\n    def __init__(self, x, y):\n        self.x = x\n        self.y = y\n    \n    def __repr__(self):\n        return f"Point({self.x}, {self.y})"  # Unambiguous\n    \n    def __str__(self):\n        return f"({self.x}, {self.y})"  # Readable\n\np = Point(3, 4)\nprint(repr(p))  # Point(3, 4)\nprint(str(p))   # (3, 4)\nprint(p)        # (3, 4) - uses __str__\nprint([p])      # [Point(3, 4)] - containers use __repr__', codeLanguage: 'python' },
      { id: 'py-010', question: 'What are Python metaclasses?', difficulty: 'hard', answer: 'Metaclasses are "classes of classes" — they define how classes behave. Every class in Python is an instance of a metaclass (default: type). When you create a class, Python calls the metaclass to construct it. Metaclasses control class creation via __new__ and __init__. Common uses: enforcing coding standards, registering classes, modifying class attributes, implementing ORMs (like Django models), singleton pattern. type(name, bases, attrs) creates classes dynamically.', code: '# type is the default metaclass\nprint(type(int))    # <class \'type\'>\nprint(type(str))    # <class \'type\'>\n\n# Custom metaclass\nclass SingletonMeta(type):\n    _instances = {}\n    \n    def __call__(cls, *args, **kwargs):\n        if cls not in cls._instances:\n            cls._instances[cls] = super().__call__(*args, **kwargs)\n        return cls._instances[cls]\n\nclass Database(metaclass=SingletonMeta):\n    def __init__(self):\n        self.connection = "connected"\n\ndb1 = Database()\ndb2 = Database()\nprint(db1 is db2)  # True - same instance', codeLanguage: 'python' },
      { id: 'py-011', question: 'How does Python memory management work?', difficulty: 'hard', answer: 'Python uses a private heap for all objects and data structures. Memory management involves: 1) Reference counting as primary mechanism — each object tracks how many references point to it. 2) Garbage collector for cyclic references — detects and collects reference cycles using generational GC (3 generations). 3) Memory pools — pymalloc allocator manages small objects (<512 bytes) in pools/arenas. 4) Interning — Python caches small integers (-5 to 256) and some strings. Memory can be managed via gc module, __slots__ to reduce per-instance memory, and weakref for weak references.', code: 'import sys\nimport gc\n\n# Reference counting\na = [1, 2, 3]\nprint(sys.getrefcount(a))  # 2 (a + getrefcount arg)\nb = a\nprint(sys.getrefcount(a))  # 3\ndel b\nprint(sys.getrefcount(a))  # 2\n\n# Cyclic reference (needs GC)\nclass Node:\n    def __init__(self):\n        self.ref = None\n\na = Node()\nb = Node()\na.ref = b  # a -> b\nb.ref = a  # b -> a (cycle!)\ndel a, b   # Reference count != 0, GC handles it\n\n# __slots__ for memory efficiency\nclass Optimized:\n    __slots__ = [\'x\', \'y\']\n    def __init__(self, x, y):\n        self.x = x\n        self.y = y', codeLanguage: 'python' },
      { id: 'py-012', question: 'Explain list comprehensions vs generator expressions.', difficulty: 'easy', answer: 'List comprehensions create a complete list in memory using [expr for item in iterable]. Generator expressions create a lazy iterator using (expr for item in iterable). List comprehensions are faster for small datasets since the list is pre-allocated. Generator expressions are memory-efficient for large datasets. List comps can be indexed and reused; generators are single-use. Nested comprehensions read left-to-right. Both support conditionals with if clause.', code: '# List comprehension - creates full list\nsquares_list = [x**2 for x in range(1000000)]  # ~8MB memory\n\n# Generator expression - lazy, minimal memory\nsquares_gen = (x**2 for x in range(1000000))   # ~120 bytes\n\n# With condition\nevens = [x for x in range(100) if x % 2 == 0]\n\n# Nested comprehension\nmatrix = [[1,2,3],[4,5,6],[7,8,9]]\nflat = [x for row in matrix for x in row]\n# Equivalent to:\n# for row in matrix:\n#     for x in row:\n#         flat.append(x)\n\nimport sys\nprint(sys.getsizeof(squares_list))  # Large\nprint(sys.getsizeof(squares_gen))   # Tiny (~120 bytes)', codeLanguage: 'python' },
      { id: 'py-013', question: 'What is duck typing in Python?', difficulty: 'easy', answer: 'Duck typing is a programming concept where the type of an object is determined by its behavior (methods and properties) rather than its explicit type. "If it walks like a duck and quacks like a duck, it\'s a duck." Python doesn\'t check types at compile time — if an object has the required method, it works regardless of its class. This enables polymorphism without inheritance. Related: Python\'s Protocol class (PEP 544) formalizes duck typing with structural subtyping for type checkers.', code: 'class Duck:\n    def quack(self):\n        print("Quack!")\n    def walk(self):\n        print("Walking...")\n\nclass Person:\n    def quack(self):\n        print("I can quack too!")\n    def walk(self):\n        print("Walking like a duck...")\n\ndef make_it_quack(thing):\n    thing.quack()  # No type check - just needs quack()\n\nmake_it_quack(Duck())    # Quack!\nmake_it_quack(Person())  # I can quack too!\n\n# Protocol (Python 3.8+)\nfrom typing import Protocol\n\nclass Quackable(Protocol):\n    def quack(self) -> None: ...', codeLanguage: 'python' },
      { id: 'py-014', question: 'What is the difference between is and == in Python?', difficulty: 'easy', answer: '== checks for value equality (calls __eq__). is checks for identity — whether two references point to the same object in memory. Use is for comparing with None, True, False (singletons). Python interns small integers (-5 to 256) and some strings, so is may return True unexpectedly for these. Always use == for value comparison and is only for identity/None checks.', code: 'a = [1, 2, 3]\nb = [1, 2, 3]\nc = a\n\nprint(a == b)   # True (same value)\nprint(a is b)   # False (different objects)\nprint(a is c)   # True (same object)\n\n# Integer interning\nx = 256\ny = 256\nprint(x is y)   # True (cached)\n\nx = 257\ny = 257\nprint(x is y)   # False (not cached) - may vary!\n\n# Always use is for None\nvalue = None\nif value is None:      # Correct\n    print("None")\n# if value == None:    # Works but not Pythonic', codeLanguage: 'python' },
      { id: 'py-015', question: 'Explain Python\'s asyncio and async/await.', difficulty: 'hard', answer: 'asyncio is Python\'s library for writing concurrent code using async/await syntax. It uses a single-threaded event loop to handle I/O-bound operations concurrently. async def defines a coroutine function. await suspends the coroutine until the awaited task completes. Key components: Event loop (scheduler), Coroutines (async functions), Tasks (scheduled coroutines), Futures (placeholders for results). asyncio.gather() runs multiple coroutines concurrently. asyncio.create_task() schedules a coroutine. It\'s NOT parallel — it\'s concurrent (one thread, cooperative multitasking).', code: 'import asyncio\n\nasync def fetch_data(url, delay):\n    print(f"Fetching {url}...")\n    await asyncio.sleep(delay)  # Simulates I/O\n    return f"Data from {url}"\n\nasync def main():\n    # Sequential: 3 seconds total\n    r1 = await fetch_data("api/users", 1)\n    r2 = await fetch_data("api/posts", 2)\n    \n    # Concurrent: ~2 seconds total (max delay)\n    results = await asyncio.gather(\n        fetch_data("api/users", 1),\n        fetch_data("api/posts", 2),\n        fetch_data("api/comments", 1),\n    )\n    print(results)\n    \n    # Task creation\n    task = asyncio.create_task(fetch_data("api/data", 1))\n    # ... do other work ...\n    result = await task\n\nasyncio.run(main())', codeLanguage: 'python' },
      { id: 'py-016', question: 'What are dataclasses in Python?', difficulty: 'medium', answer: 'Dataclasses (PEP 557, Python 3.7+) automatically generate __init__, __repr__, __eq__, and other special methods for classes that primarily store data. They reduce boilerplate code. Features: default values, field() for fine-grained control, frozen=True for immutability, slots=True (3.10+) for memory efficiency, post_init for validation, ordering with order=True. Dataclasses are mutable by default (unlike NamedTuple). They support inheritance and can be converted to dicts/tuples.', code: 'from dataclasses import dataclass, field\nfrom typing import List\n\n@dataclass(frozen=True, order=True)  # Immutable + comparable\nclass Point:\n    x: float\n    y: float\n    label: str = field(compare=False, default="origin")\n\n@dataclass\nclass Team:\n    name: str\n    members: List[str] = field(default_factory=list)\n    _size: int = field(init=False)  # Not in __init__\n    \n    def __post_init__(self):\n        self._size = len(self.members)\n\np1 = Point(1.0, 2.0)\np2 = Point(1.0, 2.0)\nprint(p1 == p2)       # True (auto __eq__)\nprint(p1)             # Point(x=1.0, y=2.0, label=\'origin\')\n# p1.x = 5.0          # FrozenInstanceError (frozen=True)', codeLanguage: 'python' },
      { id: 'py-017', question: 'What are Python descriptors?', difficulty: 'hard', answer: 'Descriptors are objects that define __get__, __set__, and/or __delete__ methods. They customize attribute access on classes. Data descriptors define both __get__ and __set__ (take priority over instance __dict__). Non-data descriptors define only __get__ (instance __dict__ takes priority). The descriptor protocol powers: properties, classmethods, staticmethods, and __slots__. They are the foundation of Python\'s attribute lookup chain.', code: 'class Validator:\n    """Data descriptor for type validation"""\n    def __init__(self, expected_type):\n        self.expected_type = expected_type\n        self.name = None\n    \n    def __set_name__(self, owner, name):\n        self.name = name\n    \n    def __get__(self, obj, objtype=None):\n        if obj is None:\n            return self\n        return obj.__dict__.get(self.name)\n    \n    def __set__(self, obj, value):\n        if not isinstance(value, self.expected_type):\n            raise TypeError(\n                f"{self.name} must be {self.expected_type.__name__}"\n            )\n        obj.__dict__[self.name] = value\n\nclass User:\n    name = Validator(str)\n    age = Validator(int)\n    \n    def __init__(self, name, age):\n        self.name = name  # Calls Validator.__set__\n        self.age = age\n\nuser = User("Viraj", 22)  # OK\n# User("Viraj", "22")     # TypeError!', codeLanguage: 'python' },
      { id: 'py-018', question: 'What is the difference between @staticmethod and @classmethod?', difficulty: 'easy', answer: '@staticmethod defines a method that doesn\'t receive the class or instance as first argument — it\'s just a regular function in the class namespace. @classmethod receives the class (cls) as first argument, not the instance. Use classmethod for alternative constructors and factory methods. Use staticmethod for utility functions that logically belong to the class but don\'t need class/instance data. Both can be called on the class or instance.', code: 'class Date:\n    def __init__(self, year, month, day):\n        self.year = year\n        self.month = month\n        self.day = day\n    \n    @classmethod\n    def from_string(cls, date_str):\n        """Alternative constructor"""\n        y, m, d = map(int, date_str.split("-"))\n        return cls(y, m, d)  # Works with subclasses!\n    \n    @staticmethod\n    def is_valid(date_str):\n        """Utility - no cls or self needed"""\n        parts = date_str.split("-")\n        return len(parts) == 3 and all(p.isdigit() for p in parts)\n    \n    def __repr__(self):\n        return f"Date({self.year}, {self.month}, {self.day})"\n\nDate.is_valid("2024-01-15")           # True\nd = Date.from_string("2024-01-15")    # Date(2024, 1, 15)', codeLanguage: 'python' },
      { id: 'py-019', question: 'What are walrus operator and its use cases?', difficulty: 'medium', answer: 'The walrus operator (:=) was introduced in Python 3.8 (PEP 572). It allows assignment within expressions (assignment expressions). Benefits: reduces redundant computation, makes while loops cleaner, works in list comprehensions with filtering. It assigns a value to a variable as part of a larger expression, avoiding the need to compute a value twice or use a separate assignment line.', code: '# Without walrus\ndata = input("Enter: ")\nwhile data != "quit":\n    process(data)\n    data = input("Enter: ")\n\n# With walrus - cleaner\nwhile (data := input("Enter: ")) != "quit":\n    process(data)\n\n# In list comprehension\nresults = [\n    cleaned\n    for text in raw_data\n    if (cleaned := text.strip().lower())  # Compute once\n    and len(cleaned) > 3\n]\n\n# With regex\nimport re\nif (match := re.search(r\'\\d+\', text)):\n    number = int(match.group())', codeLanguage: 'python' },
      { id: 'py-020', question: 'Explain Python\'s garbage collection mechanism.', difficulty: 'hard', answer: 'Python uses two mechanisms: 1) Reference Counting — every object has a counter tracking references to it; when count reaches 0, memory is freed immediately. 2) Generational Garbage Collector — handles cyclic references that reference counting can\'t detect. Objects are divided into 3 generations (0, 1, 2). New objects start in generation 0. Objects surviving a collection are promoted to the next generation. Generation 0 is collected most frequently. The gc module provides gc.collect(), gc.get_threshold(), gc.disable(). Weak references (weakref) don\'t increment reference count.', code: 'import gc\nimport weakref\n\n# Check GC thresholds\nprint(gc.get_threshold())  # (700, 10, 10)\n# Gen 0 collected every 700 allocations\n# Gen 1 every 10 Gen 0 collections\n# Gen 2 every 10 Gen 1 collections\n\n# Force collection\ncollected = gc.collect()\nprint(f"Collected {collected} objects")\n\n# Weak references\nclass Cache:\n    pass\n\nobj = Cache()\nweak = weakref.ref(obj)\nprint(weak())   # <Cache object>\ndel obj\nprint(weak())   # None - object was collected', codeLanguage: 'python' },
      { id: 'py-021', question: 'What is monkey patching in Python?', difficulty: 'medium', answer: 'Monkey patching is dynamically modifying a class or module at runtime. You can add, modify, or replace methods and attributes of existing classes/modules. Uses: testing (mocking), hot-fixing bugs without code changes, extending third-party libraries. Risks: hard to debug, can break with updates, makes code unpredictable. In testing, unittest.mock.patch() is the preferred approach over manual monkey patching.', code: 'import json\n\n# Monkey patching a class\nclass User:\n    def __init__(self, name):\n        self.name = name\n    def greet(self):\n        return f"Hello, {self.name}"\n\n# Override method at runtime\ndef new_greet(self):\n    return f"Hey there, {self.name}!"\n\nUser.greet = new_greet  # Monkey patch!\nprint(User("Viraj").greet())  # "Hey there, Viraj!"\n\n# Better approach: unittest.mock\nfrom unittest.mock import patch\n\nwith patch.object(User, \'greet\', return_value="Mocked"):\n    print(User("Test").greet())  # "Mocked"', codeLanguage: 'python' },
      { id: 'py-022', question: 'What are Python slots and when to use them?', difficulty: 'medium', answer: '__slots__ is a class variable that restricts the attributes an instance can have. By default, Python objects store attributes in a __dict__ dictionary. __slots__ replaces __dict__ with a fixed set of attribute slots, saving ~40% memory per instance. Use when: creating millions of instances, memory is critical, you want to prevent arbitrary attribute assignment. Limitations: no __dict__ (unless explicitly included), no default values in slots, complications with multiple inheritance.', code: 'import sys\n\nclass WithDict:\n    def __init__(self, x, y):\n        self.x = x\n        self.y = y\n\nclass WithSlots:\n    __slots__ = [\'x\', \'y\']\n    def __init__(self, x, y):\n        self.x = x\n        self.y = y\n\n# Memory comparison\nobj_dict = WithDict(1, 2)\nobj_slots = WithSlots(1, 2)\n\nprint(sys.getsizeof(obj_dict.__dict__))  # ~104 bytes\n# obj_slots has no __dict__\n\nobj_slots.z = 3  # AttributeError! Not in __slots__\nobj_dict.z = 3   # Works fine (dynamic)', codeLanguage: 'python' },
      { id: 'py-023', question: 'Explain the difference between process, thread, and coroutine in Python.', difficulty: 'hard', answer: 'Process: Separate memory space, true parallelism, heavyweight, created via multiprocessing module. Best for CPU-bound tasks. Thread: Shared memory space, limited by GIL for CPU-bound work, lighter than processes, created via threading module. Best for I/O-bound tasks with shared state. Coroutine: Single-threaded, cooperative multitasking via async/await, extremely lightweight, managed by event loop. Best for I/O-bound tasks with many concurrent operations (web scraping, API calls). Memory usage: Process > Thread >> Coroutine.', code: 'import asyncio\nimport threading\nimport multiprocessing\n\n# Process - true parallelism\ndef cpu_task(n):\n    return sum(i**2 for i in range(n))\n\nwith multiprocessing.Pool(4) as pool:\n    results = pool.map(cpu_task, [10**6]*4)\n\n# Thread - shared memory, GIL-limited\ndef io_task():\n    import time\n    time.sleep(1)  # GIL released during sleep\n\nthreads = [threading.Thread(target=io_task) for _ in range(10)]\nfor t in threads: t.start()\nfor t in threads: t.join()  # ~1 second total\n\n# Coroutine - lightweight concurrency\nasync def async_io_task():\n    await asyncio.sleep(1)\n\nasync def main():\n    await asyncio.gather(*[async_io_task() for _ in range(1000)])\n    # ~1 second for 1000 tasks!', codeLanguage: 'python' },
      { id: 'py-024', question: 'What is the purpose of __init__.py?', difficulty: 'easy', answer: '__init__.py marks a directory as a Python package. It runs when the package is imported. It can: define package-level variables, import submodules for convenient access, set __all__ for wildcard imports. Since Python 3.3, implicit namespace packages don\'t require __init__.py, but it\'s still best practice. Empty __init__.py files are common. It controls what\'s accessible via from package import *.', code: '# my_package/__init__.py\nfrom .module_a import ClassA\nfrom .module_b import function_b\n\n__all__ = [\'ClassA\', \'function_b\']  # Controls * imports\n__version__ = "1.0.0"\n\n# Usage\nfrom my_package import ClassA  # Works because of __init__.py\nimport my_package\nprint(my_package.__version__)  # "1.0.0"', codeLanguage: 'python' },
      { id: 'py-025', question: 'What are Python type hints and how do they work?', difficulty: 'medium', answer: 'Type hints (PEP 484) provide optional static type information. They are NOT enforced at runtime — Python remains dynamically typed. Type checkers (mypy, pyright) use them for static analysis. Benefits: better IDE support, documentation, catch bugs early. typing module provides complex types: List, Dict, Optional, Union, Callable, TypeVar, Generic. Python 3.10+ allows X | Y instead of Union[X, Y]. Python 3.12 introduced type statement for type aliases.', code: 'from typing import Optional, Union\nfrom collections.abc import Callable\n\ndef greet(name: str, age: int = 25) -> str:\n    return f"Hello {name}, age {age}"\n\ndef process(\n    items: list[int],          # Python 3.9+\n    callback: Callable[[int], bool],\n    default: int | None = None  # Python 3.10+\n) -> dict[str, list[int]]:\n    result = {"even": [], "odd": []}\n    for item in items:\n        if callback(item):\n            result["even"].append(item)\n    return result\n\n# Generic types\nfrom typing import TypeVar, Generic\nT = TypeVar(\'T\')\n\nclass Stack(Generic[T]):\n    def __init__(self) -> None:\n        self._items: list[T] = []\n    def push(self, item: T) -> None:\n        self._items.append(item)\n    def pop(self) -> T:\n        return self._items.pop()', codeLanguage: 'python' },
    ],
  },
  {
    id: 'java',
    title: 'Java',
    icon: '☕',
    summary: 'Statically-typed, object-oriented language running on JVM. Enterprise standard for backend services, Android development, and large-scale distributed systems.',
    concepts: ['JVM', 'Garbage Collection', 'Generics', 'Streams', 'Lambdas', 'Multithreading', 'Collections Framework', 'Spring', 'OOP Principles', 'Exception Handling', 'Functional Interfaces'],
    questions: [
      { id: 'java-001', question: 'What is the difference between JDK, JRE, and JVM?', difficulty: 'easy', answer: 'JVM (Java Virtual Machine): Abstract machine that executes Java bytecode. Platform-dependent (different JVM for each OS). Handles memory management, garbage collection, and security. JRE (Java Runtime Environment): JVM + standard class libraries + supporting files. Everything needed to run Java programs. JDK (Java Development Kit): JRE + development tools (javac compiler, debugger, javadoc). Everything needed to develop and run Java programs. Hierarchy: JDK ⊃ JRE ⊃ JVM.' },
      { id: 'java-002', question: 'Explain the differences between abstract class and interface in Java.', difficulty: 'medium', answer: 'Abstract class: Can have abstract and concrete methods, instance variables, constructors. Single inheritance only. Can have any access modifiers. Interface: All methods are implicitly public. Can have default and static methods (Java 8+), private methods (Java 9+). Supports multiple implementation. Variables are implicitly public static final. Use abstract class for "is-a" relationships with shared state. Use interface for "can-do" contracts and multiple type inheritance. Since Java 8, interfaces can have method implementations (default methods).', code: 'abstract class Animal {\n    protected String name;\n    \n    Animal(String name) {\n        this.name = name;\n    }\n    \n    abstract void makeSound();  // Must implement\n    \n    void breathe() {            // Concrete method\n        System.out.println(name + " breathes");\n    }\n}\n\ninterface Swimmable {\n    void swim();\n    \n    default void float_() {     // Default method (Java 8+)\n        System.out.println("Floating...");\n    }\n}\n\nclass Duck extends Animal implements Swimmable {\n    Duck() { super("Duck"); }\n    void makeSound() { System.out.println("Quack!"); }\n    public void swim() { System.out.println("Swimming!"); }\n}', codeLanguage: 'java' },
      { id: 'java-003', question: 'How does Java garbage collection work?', difficulty: 'hard', answer: 'Java GC automatically reclaims memory from unreachable objects. The heap is divided into: Young Generation (Eden + Survivor S0/S1) for new objects, Old/Tenured Generation for long-lived objects, and Metaspace for class metadata. GC algorithms: Serial GC (single-thread), Parallel GC (multi-thread), G1 GC (default since Java 9, region-based), ZGC (ultra-low latency, <1ms pauses), Shenandoah (concurrent). Minor GC collects Young Gen (fast, frequent). Major/Full GC collects Old Gen (slow, infrequent). Objects surviving multiple Minor GCs get promoted to Old Gen.' },
      { id: 'java-004', question: 'What is the difference between HashMap and ConcurrentHashMap?', difficulty: 'medium', answer: 'HashMap: Not thread-safe, allows one null key and multiple null values, fast for single-threaded use. ConcurrentHashMap: Thread-safe without synchronized keyword, no null keys/values, uses segment-level locking (Java 7) or CAS + synchronized blocks (Java 8+) for high concurrency. ConcurrentHashMap allows concurrent reads without locking and concurrent writes to different segments. Hashtable is thread-safe but locks the entire table (poor performance). Collections.synchronizedMap() wraps a map but also locks entirely.', code: 'import java.util.concurrent.ConcurrentHashMap;\nimport java.util.HashMap;\n\n// Not thread-safe\nHashMap<String, Integer> map = new HashMap<>();\nmap.put("key", 1);\nmap.put(null, 2);  // OK - allows null key\n\n// Thread-safe\nConcurrentHashMap<String, Integer> concMap = new ConcurrentHashMap<>();\nconcMap.put("key", 1);\n// concMap.put(null, 2);  // NullPointerException!\n\n// Atomic operations\nconcMap.putIfAbsent("key", 2);  // Won\'t overwrite\nconcMap.compute("key", (k, v) -> v + 1);  // Atomic update\nconcMap.merge("key", 1, Integer::sum);    // Atomic merge', codeLanguage: 'java' },
      { id: 'java-005', question: 'Explain Java Streams and their operations.', difficulty: 'medium', answer: 'Streams (Java 8+) provide a functional approach to processing collections. They support lazy evaluation and can be parallelized. Operations: Intermediate (lazy, return Stream): filter, map, flatMap, sorted, distinct, peek, limit, skip. Terminal (trigger execution): collect, forEach, reduce, count, min, max, anyMatch, allMatch, findFirst. Streams are single-use — cannot be reused after a terminal operation. parallel() converts to parallel stream for multi-core processing. Collectors provide toList(), groupingBy(), joining(), etc.', code: 'import java.util.stream.*;\nimport java.util.*;\n\nList<String> names = List.of("Viraj", "Alice", "Bob", "Anna", "Charlie");\n\n// Pipeline: filter -> map -> sort -> collect\nList<String> result = names.stream()\n    .filter(n -> n.length() > 3)\n    .map(String::toUpperCase)\n    .sorted()\n    .collect(Collectors.toList());\n// [ALICE, ANNA, CHARLIE, VIRAJ]\n\n// Reduce\nint sum = IntStream.rangeClosed(1, 100)\n    .reduce(0, Integer::sum);\n\n// GroupBy\nMap<Integer, List<String>> byLength = names.stream()\n    .collect(Collectors.groupingBy(String::length));\n\n// Parallel stream\nlong count = names.parallelStream()\n    .filter(n -> n.startsWith("A"))\n    .count();', codeLanguage: 'java' },
      { id: 'java-006', question: 'What are the SOLID principles in Java?', difficulty: 'medium', answer: 'S — Single Responsibility: A class should have only one reason to change. O — Open/Closed: Classes should be open for extension, closed for modification. L — Liskov Substitution: Subtypes must be substitutable for their base types without breaking the program. I — Interface Segregation: Clients shouldn\'t depend on interfaces they don\'t use; prefer small, specific interfaces. D — Dependency Inversion: High-level modules shouldn\'t depend on low-level modules; both should depend on abstractions. These principles lead to maintainable, flexible, testable code.' },
      { id: 'java-007', question: 'Explain the Java Memory Model and volatile keyword.', difficulty: 'hard', answer: 'The Java Memory Model (JMM) defines how threads interact through memory. Each thread has a local cache; changes may not be immediately visible to other threads. volatile keyword ensures: visibility (changes are immediately visible to all threads) and ordering (prevents instruction reordering). volatile does NOT provide atomicity for compound operations (like i++). For atomicity, use synchronized blocks or Atomic classes. The happens-before relationship guarantees memory visibility order between operations.', code: 'public class VolatileExample {\n    private volatile boolean running = true; // Visible to all threads\n    \n    public void stop() {\n        running = false;  // Immediately visible\n    }\n    \n    public void run() {\n        while (running) {  // Always reads latest value\n            // do work\n        }\n    }\n}\n\n// For atomic compound operations, use AtomicInteger\nimport java.util.concurrent.atomic.AtomicInteger;\n\nAtomicInteger counter = new AtomicInteger(0);\ncounter.incrementAndGet();        // Atomic i++\ncounter.compareAndSet(1, 2);      // CAS operation\ncounter.getAndUpdate(x -> x * 2); // Atomic update', codeLanguage: 'java' },
      { id: 'java-008', question: 'What are functional interfaces and lambda expressions?', difficulty: 'medium', answer: 'A functional interface has exactly one abstract method. It can have multiple default and static methods. @FunctionalInterface annotation enforces this contract. Lambda expressions provide concise syntax for implementing functional interfaces: (params) -> expression or (params) -> { statements }. Built-in functional interfaces: Predicate<T> (boolean test), Function<T,R> (transform), Consumer<T> (void action), Supplier<T> (provide value), BiFunction<T,U,R>, UnaryOperator<T>. Method references (::) are shorthand for lambdas.', code: '@FunctionalInterface\ninterface MathOperation {\n    double operate(double a, double b);\n}\n\n// Lambda expressions\nMathOperation add = (a, b) -> a + b;\nMathOperation multiply = (a, b) -> a * b;\n\n// Built-in functional interfaces\nimport java.util.function.*;\n\nPredicate<String> isLong = s -> s.length() > 5;\nFunction<String, Integer> toLength = String::length;  // Method ref\nConsumer<String> printer = System.out::println;\nSupplier<List<String>> listFactory = ArrayList::new;\n\n// Composing\nPredicate<String> isLongAndStartsWithA = \n    isLong.and(s -> s.startsWith("A"));', codeLanguage: 'java' },
      { id: 'java-009', question: 'What is the difference between String, StringBuilder, and StringBuffer?', difficulty: 'easy', answer: 'String: Immutable — every modification creates a new object. Stored in String Pool for interning. Thread-safe by nature. StringBuilder: Mutable, NOT thread-safe, faster. Use in single-threaded scenarios for frequent string modifications. StringBuffer: Mutable, thread-safe (synchronized methods), slower due to synchronization overhead. Use in multi-threaded scenarios. Performance: StringBuilder > StringBuffer > String concatenation in loops. String concatenation with + in loops creates many intermediate objects (use StringBuilder instead).', code: '// String - immutable\nString s = "Hello";\ns = s + " World";  // Creates new String object\n\n// StringBuilder - mutable, not thread-safe\nStringBuilder sb = new StringBuilder();\nfor (int i = 0; i < 1000; i++) {\n    sb.append(i).append(", ");  // Modifies in-place\n}\nString result = sb.toString();\n\n// StringBuffer - mutable, thread-safe\nStringBuffer sbuf = new StringBuffer();\nsbuf.append("Thread-safe ");  // Synchronized\nsbuf.append("operations");', codeLanguage: 'java' },
      { id: 'java-010', question: 'Explain checked vs unchecked exceptions in Java.', difficulty: 'easy', answer: 'Checked Exceptions: Compiler-enforced — must be caught or declared with throws. Extend Exception (not RuntimeException). Examples: IOException, SQLException, FileNotFoundException. Used for recoverable conditions. Unchecked Exceptions: Not checked at compile time. Extend RuntimeException. Examples: NullPointerException, ArrayIndexOutOfBoundsException, IllegalArgumentException. Represent programming bugs. Errors: Extend Error, represent serious JVM issues (OutOfMemoryError, StackOverflowError). Should NOT be caught normally.' },
      { id: 'java-011', question: 'What is the difference between == and equals() in Java?', difficulty: 'easy', answer: '== compares references (memory addresses) for objects and values for primitives. equals() compares content/values (when properly overridden). String pool: string literals with same value share the same reference, so == works. But new String() creates a new object. Always override equals() with hashCode() (contract: equal objects must have equal hash codes). Best practice: always use equals() for object comparison.', code: 'String a = "Hello";         // String pool\nString b = "Hello";         // Same reference from pool\nString c = new String("Hello"); // New object\n\nSystem.out.println(a == b);       // true (same pool ref)\nSystem.out.println(a == c);       // false (different objects)\nSystem.out.println(a.equals(c));  // true (same content)\n\n// Custom equals/hashCode\npublic class Person {\n    String name;\n    int age;\n    \n    @Override\n    public boolean equals(Object o) {\n        if (this == o) return true;\n        if (!(o instanceof Person p)) return false;\n        return age == p.age && Objects.equals(name, p.name);\n    }\n    \n    @Override\n    public int hashCode() {\n        return Objects.hash(name, age);\n    }\n}', codeLanguage: 'java' },
      { id: 'java-012', question: 'Explain Java Generics and type erasure.', difficulty: 'hard', answer: 'Generics provide compile-time type safety for collections and classes. Type erasure: generic type information is removed at compile time — bytecode has no generic types. Implications: can\'t create generic arrays (new T[]), can\'t use instanceof with generics, can\'t overload methods differing only in generic type. Bounded types: <T extends Number> restricts T. Wildcards: ? (unknown), ? extends T (upper bound), ? super T (lower bound). PECS principle: Producer Extends, Consumer Super.', code: '// Generic class\npublic class Box<T> {\n    private T value;\n    public void set(T value) { this.value = value; }\n    public T get() { return value; }\n}\n\n// Bounded type\npublic <T extends Comparable<T>> T max(T a, T b) {\n    return a.compareTo(b) >= 0 ? a : b;\n}\n\n// Wildcards - PECS\npublic void process(\n    List<? extends Number> producer,  // Read from\n    List<? super Integer> consumer    // Write to\n) {\n    for (Number n : producer) {       // Can read as Number\n        consumer.add(n.intValue());   // Can write Integer\n    }\n}\n\n// Type erasure - at runtime:\n// Box<String> and Box<Integer> are both just Box\nBox<String> strBox = new Box<>();\nBox<Integer> intBox = new Box<>();\nSystem.out.println(strBox.getClass() == intBox.getClass()); // true!', codeLanguage: 'java' },
      { id: 'java-013', question: 'What is the CompletableFuture in Java?', difficulty: 'hard', answer: 'CompletableFuture (Java 8+) provides asynchronous, non-blocking computation with a rich API for composing and combining futures. Key methods: supplyAsync() — async computation returning value, thenApply() — transform result, thenCompose() — chain futures (flatMap), thenCombine() — combine two futures, exceptionally() — handle errors, allOf/anyOf — wait for multiple futures. It implements both Future and CompletionStage interfaces. Default executor is ForkJoinPool.commonPool().', code: 'import java.util.concurrent.CompletableFuture;\n\nCompletableFuture<String> future = CompletableFuture\n    .supplyAsync(() -> fetchUser("123\"))        // Async\n    .thenApply(user -> user.getName())           // Transform\n    .thenCompose(name -> fetchProfile(name))     // Chain\n    .exceptionally(ex -> "fallback");            // Error\n\n// Combine multiple futures\nCompletableFuture<String> userFuture = fetchUserAsync();\nCompletableFuture<String> orderFuture = fetchOrderAsync();\n\nCompletableFuture<String> combined = userFuture\n    .thenCombine(orderFuture, (user, order) -> \n        user + " ordered " + order);\n\n// Wait for all\nCompletableFuture.allOf(future1, future2, future3)\n    .thenRun(() -> System.out.println("All done!"));', codeLanguage: 'java' },
    ],
  },
  {
    id: 'typescript',
    title: 'TypeScript',
    icon: '📘',
    summary: 'Statically-typed superset of JavaScript. Adds type safety, interfaces, generics, and advanced type system features to JavaScript for large-scale application development.',
    concepts: ['Type System', 'Interfaces', 'Generics', 'Union/Intersection Types', 'Type Guards', 'Mapped Types', 'Conditional Types', 'Decorators', 'Enums', 'Utility Types'],
    questions: [
      { id: 'ts-001', question: 'What is the difference between interface and type in TypeScript?', difficulty: 'medium', answer: 'Both define object shapes, but: Interfaces support declaration merging (same-name interfaces auto-merge), extends for inheritance, and are preferred for public API contracts. Types support unions (A | B), intersections (A & B), mapped types, conditional types, and primitives. Types use & for combining, interfaces use extends. Best practice: use interface for object shapes and public APIs, type for unions, tuples, and complex type operations. Since TypeScript 4.2+, error messages reference type aliases directly.', code: '// Interface - declaration merging\ninterface User {\n  name: string;\n  age: number;\n}\ninterface User {  // Merged!\n  email: string;\n}\n// User now has name, age, AND email\n\n// Type - unions, intersections, mapped\ntype Status = "active" | "inactive" | "pending";\ntype Response<T> = { data: T; error: null } | { data: null; error: string };\n\ntype ReadOnly<T> = {\n  readonly [K in keyof T]: T[K];\n};\n\n// Intersection\ntype Admin = User & { role: "admin"; permissions: string[] };\n\n// Interface extends\ninterface Employee extends User {\n  company: string;\n}', codeLanguage: 'typescript' },
      { id: 'ts-002', question: 'Explain TypeScript generics with examples.', difficulty: 'medium', answer: 'Generics allow creating reusable components that work with multiple types while maintaining type safety. They act as type variables (placeholders) resolved at usage time. Constraints use extends to limit acceptable types. Default types provide fallbacks. Generic constraints can reference other type parameters using keyof. Common patterns: generic functions, generic classes, generic interfaces, and utility types.', code: '// Generic function\nfunction identity<T>(arg: T): T {\n  return arg;\n}\nconst str = identity<string>("hello"); // string\nconst num = identity(42);              // inferred as number\n\n// Generic with constraint\nfunction getProperty<T, K extends keyof T>(obj: T, key: K): T[K] {\n  return obj[key];\n}\nconst user = { name: "Viraj", age: 22 };\ngetProperty(user, "name");  // string\n// getProperty(user, "foo"); // Error!\n\n// Generic class\nclass ApiResponse<T> {\n  constructor(\n    public data: T,\n    public status: number,\n    public timestamp = new Date()\n  ) {}\n  \n  map<U>(fn: (data: T) => U): ApiResponse<U> {\n    return new ApiResponse(fn(this.data), this.status);\n  }\n}', codeLanguage: 'typescript' },
      { id: 'ts-003', question: 'What are TypeScript utility types?', difficulty: 'medium', answer: 'Built-in generic types that transform existing types: Partial<T> — all properties optional, Required<T> — all required, Readonly<T> — all readonly, Pick<T, K> — subset of properties, Omit<T, K> — exclude properties, Record<K, V> — key-value map, Exclude<T, U> — remove types from union, Extract<T, U> — keep matching types, NonNullable<T> — remove null/undefined, ReturnType<T> — function return type, Parameters<T> — function parameter types, Awaited<T> — unwrap Promise type.', code: 'interface User {\n  id: number;\n  name: string;\n  email: string;\n  age: number;\n}\n\n// Partial - all optional (for updates)\ntype UserUpdate = Partial<User>;\n// { id?: number; name?: string; ... }\n\n// Pick - subset\ntype UserPreview = Pick<User, "id" | "name">;\n// { id: number; name: string }\n\n// Omit - exclude\ntype CreateUser = Omit<User, "id">;\n// { name: string; email: string; age: number }\n\n// Record\ntype UserRoles = Record<string, "admin" | "user" | "guest">;\n\n// ReturnType\nfunction fetchUser() { return { id: 1, name: "Viraj" }; }\ntype FetchResult = ReturnType<typeof fetchUser>;\n// { id: number; name: string }', codeLanguage: 'typescript' },
      { id: 'ts-004', question: 'What are conditional types in TypeScript?', difficulty: 'hard', answer: 'Conditional types select a type based on a condition: T extends U ? X : Y. They enable type-level programming and conditional logic. Combined with infer, they can extract types from complex structures. Distributive conditional types automatically distribute over unions. Common use: creating type-safe utility types, extracting return types, unwrapping promises, and building type-safe APIs.', code: '// Basic conditional type\ntype IsString<T> = T extends string ? true : false;\ntype A = IsString<string>;  // true\ntype B = IsString<number>;  // false\n\n// With infer - extract types\ntype UnwrapPromise<T> = T extends Promise<infer U> ? U : T;\ntype C = UnwrapPromise<Promise<string>>; // string\ntype D = UnwrapPromise<number>;          // number\n\n// Extract array element type\ntype ElementOf<T> = T extends (infer E)[] ? E : never;\ntype E = ElementOf<string[]>; // string\n\n// Distributive conditional types\ntype Exclude<T, U> = T extends U ? never : T;\ntype F = Exclude<"a" | "b" | "c", "a">; // "b" | "c"\n// Distributes: ("a" extends "a" ? never : "a") |\n//              ("b" extends "a" ? never : "b") | ...\n// = never | "b" | "c" = "b" | "c"', codeLanguage: 'typescript' },
      { id: 'ts-005', question: 'What are type guards in TypeScript?', difficulty: 'medium', answer: 'Type guards narrow a type within a conditional block. Built-in: typeof (primitives), instanceof (classes), in (property check), equality checks. Custom type guards use "is" syntax: param is Type. Discriminated unions use a common literal property to distinguish types. The satisfies operator (TS 4.9+) validates a type without widening. Type assertions (as Type) override the compiler but are unsafe.', code: '// typeof guard\nfunction process(value: string | number) {\n  if (typeof value === "string") {\n    return value.toUpperCase();  // narrowed to string\n  }\n  return value.toFixed(2);       // narrowed to number\n}\n\n// Custom type guard\ninterface Fish { swim(): void }\ninterface Bird { fly(): void }\n\nfunction isFish(pet: Fish | Bird): pet is Fish {\n  return (pet as Fish).swim !== undefined;\n}\n\n// Discriminated union\ntype Shape =\n  | { kind: "circle"; radius: number }\n  | { kind: "rect"; width: number; height: number };\n\nfunction area(s: Shape): number {\n  switch (s.kind) {\n    case "circle": return Math.PI * s.radius ** 2;\n    case "rect":   return s.width * s.height;\n  }\n}', codeLanguage: 'typescript' },
      { id: 'ts-006', question: 'What is the difference between any, unknown, and never?', difficulty: 'medium', answer: 'any: Disables type checking entirely. Any operation is allowed. Avoid in production code. unknown: Type-safe counterpart of any. Must narrow the type before using it. Safer than any. never: Represents values that never occur. Used for: exhaustive checks, functions that never return (throw or infinite loop), impossible types. Type hierarchy: never ⊂ all types ⊂ unknown. any bypasses the type system entirely.', code: '// any - no type safety\nlet a: any = "hello";\na.nonExistentMethod();  // No error! (runtime crash)\n\n// unknown - must narrow first\nlet b: unknown = "hello";\n// b.toUpperCase();  // Error!\nif (typeof b === "string") {\n  b.toUpperCase();    // OK after narrowing\n}\n\n// never - impossible values\nfunction throwError(msg: string): never {\n  throw new Error(msg);  // Never returns\n}\n\n// Exhaustive check\ntype Color = "red" | "blue";\nfunction paint(c: Color) {\n  switch (c) {\n    case "red": return "#ff0000";\n    case "blue": return "#0000ff";\n    default:\n      const _exhaustive: never = c;  // Error if case missed\n      return _exhaustive;\n  }\n}', codeLanguage: 'typescript' },
      { id: 'ts-007', question: 'What are mapped types in TypeScript?', difficulty: 'hard', answer: 'Mapped types create new types by transforming properties of existing types. Syntax: { [K in keyof T]: NewType }. You can add/remove modifiers (+/- readonly, +/- ?). Key remapping (as clause) allows renaming keys. Template literal types can create string-based mapped types. They power utility types like Partial, Required, Readonly, and Record.', code: '// Basic mapped type\ntype Optional<T> = {\n  [K in keyof T]?: T[K];\n};\n\n// Remove readonly\ntype Mutable<T> = {\n  -readonly [K in keyof T]: T[K];\n};\n\n// Key remapping with `as`\ntype Getters<T> = {\n  [K in keyof T as `get${Capitalize<string & K>}`]: () => T[K];\n};\n\ninterface Person { name: string; age: number; }\ntype PersonGetters = Getters<Person>;\n// { getName: () => string; getAge: () => number }\n\n// Filter keys\ntype OnlyStrings<T> = {\n  [K in keyof T as T[K] extends string ? K : never]: T[K];\n};\ntype StringProps = OnlyStrings<Person>;\n// { name: string } - age filtered out', codeLanguage: 'typescript' },
      { id: 'ts-008', question: 'What are enums in TypeScript and when to use them?', difficulty: 'easy', answer: 'Enums define a set of named constants. Numeric enums auto-increment from 0 (or specified value). String enums require explicit values. const enums are inlined at compile time (no runtime object). Enums generate real JavaScript objects (unlike most TS features). Alternatives: union types ("a" | "b" | "c") are often preferred — they don\'t generate runtime code and provide better type narrowing. Use enums for numeric constants or when you need reverse mapping; use unions for string literals.', code: '// Numeric enum\nenum Direction {\n  Up,      // 0\n  Down,    // 1\n  Left,    // 2\n  Right,   // 3\n}\n\n// String enum\nenum Status {\n  Active = "ACTIVE",\n  Inactive = "INACTIVE",\n  Pending = "PENDING",\n}\n\n// const enum - inlined, no runtime object\nconst enum Color {\n  Red = "#ff0000",\n  Blue = "#0000ff",\n}\nconst c = Color.Red;  // Compiled to: const c = "#ff0000"\n\n// Often better: union type\ntype StatusType = "active" | "inactive" | "pending";\n// No runtime overhead, better tree-shaking', codeLanguage: 'typescript' },
    ],
  },
  {
    id: 'cpp',
    title: 'C++',
    icon: '⚡',
    summary: 'High-performance, statically-typed language with manual memory management. Used in systems programming, game engines, embedded systems, and competitive programming.',
    concepts: ['Pointers', 'References', 'RAII', 'Smart Pointers', 'Move Semantics', 'Templates', 'STL', 'Virtual Functions', 'Operator Overloading', 'Memory Layout', 'Multithreading'],
    questions: [
      { id: 'cpp-001', question: 'What is the difference between pointers and references in C++?', difficulty: 'easy', answer: 'Pointers: Hold memory addresses, can be null, can be reassigned, support arithmetic (++, --), need dereferencing (*) to access value, can point to nothing or be dangling. References: Aliases for existing variables, cannot be null, cannot be reassigned (bound at initialization), no dereferencing needed, must be initialized at declaration. References are safer and preferred for function parameters. Pointers are needed for dynamic memory, optional parameters, and data structures like linked lists.', code: 'int x = 10;\n\n// Pointer\nint* ptr = &x;    // Stores address of x\n*ptr = 20;        // Dereference to modify x\nptr = nullptr;    // Can be null\nptr++;            // Pointer arithmetic\n\n// Reference\nint& ref = x;     // Alias for x\nref = 30;         // Directly modifies x\n// int& ref2;     // Error! Must initialize\n// ref = y;       // Doesn\'t rebind - assigns y\'s value to x\n\n// Function parameters\nvoid byPointer(int* p) { if (p) *p = 100; }  // Nullable\nvoid byReference(int& r) { r = 100; }        // Always valid\nvoid byConstRef(const int& r) { /* read-only */ }', codeLanguage: 'cpp' },
      { id: 'cpp-002', question: 'What are smart pointers in C++?', difficulty: 'medium', answer: 'Smart pointers (C++11) manage dynamic memory automatically using RAII. unique_ptr: Exclusive ownership — only one unique_ptr can own a resource. No copy, only move. Zero overhead. Most common choice. shared_ptr: Shared ownership via reference counting. Multiple shared_ptrs can own the same resource. Freed when last owner is destroyed. Has overhead (ref count, control block). weak_ptr: Non-owning observer of shared_ptr. Breaks circular references. Must lock() to access. Use unique_ptr by default, shared_ptr when ownership must be shared, weak_ptr to break cycles.', code: '#include <memory>\n\n// unique_ptr - exclusive ownership\nauto uptr = std::make_unique<int>(42);\n// auto copy = uptr;              // Error! No copying\nauto moved = std::move(uptr);     // OK - transfer ownership\n\n// shared_ptr - shared ownership\nauto sptr1 = std::make_shared<std::string>("Hello");\nauto sptr2 = sptr1;  // ref_count = 2\nsptr1.reset();       // ref_count = 1, not freed\nsptr2.reset();       // ref_count = 0, freed!\n\n// weak_ptr - break circular references\nstruct Node {\n    std::shared_ptr<Node> next;\n    std::weak_ptr<Node> prev;  // Weak to prevent cycle!\n};\n\nauto a = std::make_shared<Node>();\nauto b = std::make_shared<Node>();\na->next = b;\nb->prev = a;  // Weak ref - no ref count increase', codeLanguage: 'cpp' },
      { id: 'cpp-003', question: 'Explain move semantics and rvalue references.', difficulty: 'hard', answer: 'Move semantics (C++11) enable transferring resources instead of copying. Rvalue references (T&&) bind to temporary values. Move constructor and move assignment operator "steal" resources from the source, leaving it in a valid but unspecified state. std::move() casts an lvalue to an rvalue reference (doesn\'t actually move). Benefits: eliminates unnecessary copies for large objects (vectors, strings), enables efficient return of local objects, powers perfect forwarding. Rule of 5: if you define any of destructor, copy constructor, copy assignment, move constructor, move assignment — define all 5.', code: 'class Buffer {\n    int* data;\n    size_t size;\npublic:\n    // Constructor\n    Buffer(size_t s) : data(new int[s]), size(s) {}\n    \n    // Copy constructor (expensive)\n    Buffer(const Buffer& other) : data(new int[other.size]), size(other.size) {\n        std::copy(other.data, other.data + size, data);\n    }\n    \n    // Move constructor (cheap - steal resources)\n    Buffer(Buffer&& other) noexcept \n        : data(other.data), size(other.size) {\n        other.data = nullptr;  // Leave source valid\n        other.size = 0;\n    }\n    \n    // Move assignment\n    Buffer& operator=(Buffer&& other) noexcept {\n        if (this != &other) {\n            delete[] data;\n            data = other.data;\n            size = other.size;\n            other.data = nullptr;\n            other.size = 0;\n        }\n        return *this;\n    }\n    \n    ~Buffer() { delete[] data; }\n};\n\nBuffer a(1000);\nBuffer b = std::move(a);  // Move, not copy!', codeLanguage: 'cpp' },
      { id: 'cpp-004', question: 'What is RAII in C++?', difficulty: 'medium', answer: 'RAII (Resource Acquisition Is Initialization) ties resource lifetime to object lifetime. Resources (memory, files, locks, sockets) are acquired in constructors and released in destructors. When the object goes out of scope, the destructor automatically releases resources. Benefits: exception-safe resource management, no resource leaks, no manual cleanup needed. RAII is the foundation of smart pointers, lock_guard, unique_lock, fstream, and other standard library types.', code: '// RAII file handler\nclass FileHandler {\n    FILE* file;\npublic:\n    FileHandler(const char* path, const char* mode)\n        : file(fopen(path, mode)) {\n        if (!file) throw std::runtime_error("Cannot open file");\n    }\n    ~FileHandler() {\n        if (file) fclose(file);  // Always closes!\n    }\n    // ... read/write methods ...\n};\n\nvoid process() {\n    FileHandler f("data.txt", "r");  // Acquired\n    // ... use file ...\n    // If exception thrown here, destructor STILL runs!\n}  // f destroyed here -> file closed automatically\n\n// RAII mutex lock\n{\n    std::lock_guard<std::mutex> lock(mtx);  // Acquired\n    // ... critical section ...\n}  // lock released automatically', codeLanguage: 'cpp' },
      { id: 'cpp-005', question: 'What is the difference between virtual and pure virtual functions?', difficulty: 'medium', answer: 'Virtual functions enable runtime polymorphism via vtable dispatch. A virtual function has a default implementation in the base class that derived classes can override. A pure virtual function (= 0) has NO implementation in the base class — derived classes MUST override it. A class with at least one pure virtual function is abstract and cannot be instantiated. The vtable is a lookup table of function pointers; each class with virtual functions has one. Virtual destructor is needed in base classes to ensure proper cleanup of derived objects.', code: 'class Shape {\npublic:\n    virtual double area() const = 0;  // Pure virtual (abstract)\n    virtual void draw() const {        // Virtual (has default)\n        std::cout << "Drawing shape" << std::endl;\n    }\n    virtual ~Shape() = default;        // Virtual destructor!\n};\n\nclass Circle : public Shape {\n    double radius;\npublic:\n    Circle(double r) : radius(r) {}\n    double area() const override { return 3.14159 * radius * radius; }\n    void draw() const override { std::cout << "Drawing circle" << std::endl; }\n};\n\n// Shape s;                     // Error! Abstract class\nCircle c(5.0);\nShape* ptr = &c;               // Polymorphism\nstd::cout << ptr->area();      // 78.54 (vtable dispatch)\nptr->draw();                   // "Drawing circle"', codeLanguage: 'cpp' },
      { id: 'cpp-006', question: 'What is the difference between stack and heap memory in C++?', difficulty: 'medium', answer: 'Stack: Automatic storage, LIFO order, very fast allocation/deallocation, limited size (typically 1-8 MB), scope-based lifetime, stores local variables and function call info. Heap: Dynamic storage via new/malloc, manual management (or smart pointers), larger but slower, fragmentation possible, accessible anywhere via pointer. Stack overflow occurs with deep recursion or large local arrays. Heap leaks occur when forgetting to delete. Modern C++: prefer stack allocation and smart pointers over raw new/delete.', code: '// Stack allocation - automatic, fast\nvoid stackExample() {\n    int x = 42;              // Stack\n    int arr[100];            // Stack\n    std::string s = "hello"; // Stack (but string data may be on heap)\n}  // All automatically freed\n\n// Heap allocation - manual, flexible\nvoid heapExample() {\n    int* p = new int(42);              // Heap\n    int* arr = new int[10000000];      // Heap (too large for stack)\n    \n    delete p;       // Must free!\n    delete[] arr;   // Must use delete[] for arrays!\n}\n\n// Modern C++ - prefer smart pointers\nvoid modernExample() {\n    auto p = std::make_unique<int>(42);           // Heap, auto-freed\n    auto arr = std::make_unique<int[]>(10000000); // Heap, auto-freed\n}  // Smart pointers handle cleanup', codeLanguage: 'cpp' },
      { id: 'cpp-007', question: 'What are templates in C++?', difficulty: 'medium', answer: 'Templates enable generic programming — writing code that works with any type. Function templates create generic functions. Class templates create generic classes. Template specialization allows custom behavior for specific types. Templates are instantiated at compile time (no runtime overhead). Variadic templates (C++11) accept variable number of type parameters. SFINAE (Substitution Failure Is Not An Error) and concepts (C++20) constrain template parameters. Templates can cause code bloat (each instantiation generates new code).', code: '// Function template\ntemplate<typename T>\nT maximum(T a, T b) {\n    return (a > b) ? a : b;\n}\nmaximum(3, 7);        // int version\nmaximum(3.14, 2.72);  // double version\n\n// Class template\ntemplate<typename T, int Size>\nclass Array {\n    T data[Size];\npublic:\n    T& operator[](int i) { return data[i]; }\n    constexpr int size() const { return Size; }\n};\n\nArray<int, 10> arr;    // Fixed-size int array\n\n// C++20 Concepts\ntemplate<typename T>\nconcept Addable = requires(T a, T b) { a + b; };\n\ntemplate<Addable T>\nT add(T a, T b) { return a + b; }', codeLanguage: 'cpp' },
      { id: 'cpp-008', question: 'Explain the difference between new/delete and malloc/free.', difficulty: 'easy', answer: 'new/delete: C++ operators. Call constructors/destructors. Type-safe (returns correct pointer type). Can be overloaded. Throw std::bad_alloc on failure. malloc/free: C functions. Don\'t call constructors/destructors. Return void* (needs cast). Cannot be overloaded. Return NULL on failure. Don\'t mix: never use free on new\'d memory or delete on malloc\'d memory. In modern C++, prefer smart pointers over both.', code: '// C++ new/delete\nclass Widget {\npublic:\n    Widget() { std::cout << "Constructed!\\n"; }\n    ~Widget() { std::cout << "Destroyed!\\n"; }\n};\n\nWidget* w = new Widget();     // Calls constructor\ndelete w;                      // Calls destructor\n\nint* arr = new int[100];       // Array\ndelete[] arr;                  // Must use delete[]\n\n// C malloc/free\n#include <cstdlib>\nWidget* w2 = (Widget*)malloc(sizeof(Widget)); // NO constructor!\nfree(w2);                                      // NO destructor!\n\n// Modern C++ - avoid both!\nauto w3 = std::make_unique<Widget>();  // Best practice', codeLanguage: 'cpp' },
    ],
  },
];
